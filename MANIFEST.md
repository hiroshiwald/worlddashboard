## Modules

| Name | Purpose | Key Exports |
|------|---------|-------------|
| `DESIGN.md` | Design philosophy and the product's four-decision spine (surface the non-obvious, evidence one click away, zero configuration, honest time) — experience discipline; CLAUDE.md owns code discipline | — |
| `src/app/api/sources/route.ts` | API endpoint that orchestrates feed fetching and returns aggregated items | `GET` handler |
| `src/app/api/ingest/route.ts` | Thin authenticated wrapper around `run-ingest.ts`'s `runIngest`: auth via `x-ingest-key` or `Authorization: Bearer` (`CRON_SECRET`), then wraps the shared result in a `NextResponse` | `POST`, `GET` handlers |
| `src/app/api/tick/route.ts` | Self-healing tick: unauthenticated, no request body read; one optional query flag, `?manual=1` (exact match, any other value or param ignored). 503 if `DATABASE_URL` unset; `{triggered:false, reason:"fresh"}` if the newest article is under the active freshness threshold old (`tick.ts`'s `isRecent` + `selectFreshnessThreshold` — 15min passive, 10min when `?manual=1`); otherwise attempts `tick.ts`'s atomic lock and either runs `run-ingest.ts`'s `runIngest` (winner: `{triggered:true, ...stats}`) or returns `{triggered:false, reason:"locked"}` (loser) | `POST`, `GET` handlers |
| `src/app/api/articles/route.ts` | DB-backed read path — cluster heads only, `days`/`category` filters, 503 when unconfigured/empty. Each item carries `updated_at` and `cluster_size` (`COUNT(*)` per cluster) from the `cluster_updates` CTE. EM-2b: `?titleExact=` is a second GET mode, checked before the cluster-heads/empty-table logic — exact (not fuzzy) title match against ANY article (not just cluster heads, since a candidate's sample title may belong to a dup member), scoped to the last 30 days (mirrors `sweepRetention`'s own retention window) via `make_interval`, newest `first_seen_at` first, `{link}` only; 400 on a blank/oversized title, 404 on no match — backs the candidate queue's linked sample titles (spine 2) | `GET` handler |
| `src/app/api/candidates/route.ts` | Review queue: promotable unresolved entity names, accept/merge/dismiss actions. GET response includes `contexts` (LLM role/context phrases) and `coEntities` (same-article resolved entity names). Accept-type whitelist is the full 15-type ontology. M006: after a successful accept, `inlineFameCheck` runs `fame-sweep.ts`'s shared `checkAndWriteEntityFame` for the newly-tracked (or conflict-resolved) entity, 4s-per-call-capped; any failure is caught and logged, never surfacing as a route error — the entity stays `fame='unknown'` and the next sweep retries it. `acceptCandidate`/`resolveConflictedCandidate` now return the entity's real current `aliases` (empty for a fresh insert, fetched for a conflict-resolved pre-existing entity) alongside its id, so the alias merge never clobbers aliases a pre-existing entity already had. Response contract unchanged (`{ok: true}`). M007: `loadCandidate`/`acceptCandidate`/`dismissCandidate`/`inlineFameCheck` (plus `TYPES`, `isBoundedNonEmpty`, `CandidateSnapshot`, `AcceptedEntity`, `CONFLICT_MESSAGE`, `deleteCandidate`) moved to the new `src/lib/server/candidate-actions.ts` and are imported back in here — Next.js's App Router only allows a `route.ts` file to export recognized HTTP handlers and route segment config, so exporting them directly from this file (the original plan) fails `next build`'s route type check; moving them to a plain lib module is the only way both this route and `/api/candidates/bulk` share the logic without duplicating it. No behavior change — GET/POST are otherwise identical | `GET`, `POST` handlers |
| `src/app/api/candidates/bulk/route.ts` | Bulk triage for the ~793-item Review queue: `POST {action: 'accept'\|'dismiss', items: [{nameNorm, type?}]}`, capped at 100 items (400 above, and 400 on an empty array). Each item is processed independently via `candidate-actions.ts`'s `loadCandidate`/`acceptCandidate`/`dismissCandidate`/`inlineFameCheck` (zero duplicated logic, shared with `/api/candidates`) and resolves to its own `{nameNorm, ok, error?}` — a malformed item, a not-found candidate, or a naming conflict never aborts the batch, so a partial failure is always visible in the response, never silently swallowed. `/api/candidates`'s own external contract is untouched | `POST` handler |
| `src/lib/server/candidate-actions.ts` | M007: the accept/dismiss/inline-fame-check logic extracted out of `/api/candidates/route.ts` so `/api/candidates/bulk` can reuse it without duplication — moved here (rather than exported from the route file itself) because Next.js's App Router rejects any `route.ts` export that isn't a recognized HTTP handler or route segment config. Behavior is byte-for-byte identical to the pre-extraction code: `TYPES` (the 15-type accept whitelist), `isBoundedNonEmpty`, `CandidateSnapshot`, `loadCandidate`, `deleteCandidate`, `AcceptedEntity`, `acceptCandidate` (ON CONFLICT DO NOTHING + conflict-resolution fallback), `dismissCandidate`, `inlineFameCheck` (runs `fame-sweep.ts`'s `checkAndWriteEntityFame`, non-fatal on failure), `CONFLICT_MESSAGE` | `TYPES`, `isBoundedNonEmpty`, `CandidateSnapshot`, `loadCandidate`, `deleteCandidate`, `AcceptedEntity`, `acceptCandidate`, `dismissCandidate`, `inlineFameCheck`, `CONFLICT_MESSAGE` |
| `src/app/api/entities/route.ts` | Three-mode `GET` contract, checked in order: `?view=stats` (EM-2a, new) returns `entity-admin.ts`'s `loadEntityStats` shape directly, unconditionally ahead of the other two modes since it takes no other params. Otherwise `?name=` resolves a single entity by canonical_name or alias, byte-for-byte unchanged. M007: any OTHER request (`?name=` entirely absent) is list/search mode — backed by `entity-admin.ts`'s `searchEntities`, `?q=&status=&fame=&fameChecked=&fameLocked=&sort=&limit=&offset=` -> `{entities, total}`; sort defaults to `last_seen` desc (Phase 3a — was `ORDER BY id ASC`); each entity row carries `lastSeenAt`/`mentions7d`/`sources7d` and `role`/`roleReasons` (via `searchEntities`'s own `attachEntityRoles` call — no route-level change); a malformed filter or sort 400s via `searchEntities`'s thrown `ValidationError` rather than being coerced | `GET` handler |
| `src/app/api/entities/[id]/route.ts` | `GET`: entity profile + 7-day hourly mention series + last 20 articles + top 10 co-occurring entities + directed relations (`incoming`/`outgoing`, one query each). Phase 3a: `entity` now carries every entities column via `toEntityAdminJson` (fame/fameLocked/wikiTitle/wikiSitelinks/wikiPageviewsMonthly/fameCheckedAt/aliases — previously omitted, hiding the fame verdict from the detail view) plus `lastSeenAt`; response gains top-level `activity: {mentions30d, sources30d}` (entity-admin.ts's shared window loaders, 30-day window); relations gain `evidence: {title, link} | null` (LEFT JOIN on `evidence_article_id`, cluster heads only — null when the FK is null, the article aged out, or was demoted to a duplicate member); edges gain `firstSeenAt`; `entity` also carries `role`/`roleReasons` via `attachEntityRoles`, run inside the same `Promise.all` batch as the route's other queries. M007: `PATCH` accepts any subset of `{type, status, fame, fameLocked}` — `type` validated against the full 15-type ontology, `status` freely reversible `tracked`/`dismissed` (no state-machine gate), setting `fame` also sets `fame_locked=true` (a human verdict is final until a human changes it) unless the request also gives an explicit `fameLocked`, which always wins — the `{fameLocked: false}` escape releases an entity back to `fame-sweep.ts`. One `UPDATE ... SET col = COALESCE($n, col) ... RETURNING` query applies the partial update (verified against a real local Postgres); 404 on an unknown id; the updated row is shaped via `entity-admin.ts`'s shared `toEntityAdminJson`, so it always carries its fame evidence fields | `GET`, `PATCH` handlers |
| `src/app/api/signals/route.ts` | Signal lifecycle: `GET` filtered by state (default new,seen,promoted), severity-ordered, entity names resolved; `POST {id, action}` transitions state (404 unknown id, 409 illegal transition). Unauthenticated (see Invariants) | `GET`, `POST` handlers |
| `src/app/api/brief/route.ts` | Bounded daily-brief payload: capped active signals, newly-tracked entities, recency-ranked top stories. Edge-cached 5min, 503 when unconfigured | `GET` handler |
| `src/app/api/developments/route.ts` | Uncapped developments list: `GET ?limit=` (default 50, bounds [1,100], 400 on invalid — reject, never clamp) returns `{developments, diagnostics, warmup, generatedAt}` via `getDevelopmentsDetailed`; during warm-up returns `{developments: [], diagnostics: null}` without running the engine. Edge-cached 5min (same header as `/api/brief`), 503 when unconfigured. Duplicates the tiny system-epoch query per the brief.ts/api-signals convention rather than importing a private detector helper | `GET` handler |
| `src/lib/server/db.ts` | Thin Postgres query helper over `@neondatabase/serverless` | `getSql`, `Sql`, `SqlRow` |
| `src/lib/server/run-ingest.ts` | The ingest pipeline itself, extracted so `/api/ingest` and `/api/tick` share one implementation: fetches all feeds, persists to Postgres, sweeps retention, runs entity extraction, detects and persists signals, sweeps entity fame. Returns a plain `{status, body}` result (no `NextResponse`, no auth) — each caller shapes its own response and owns its own access control. M006: `runFameSweepStage` runs `fame-sweep.ts`'s `runFameSweep` after the detect-signals stage, but deliberately NOT through `runStage` — per FABLE-ROADMAP.md §14 the fame layer must never be an ingest dependency, so unlike every other stage a sweep failure is caught locally, logged loudly, and reported as `fameSweep: null` instead of aborting the run. M008: no code change here — `counts.fameSweep` already threads `fame-sweep.ts`'s whole `FameSweepStats` object through untouched, so its new `failureReasons` tally and optional `abortedReason` marker (see that module) land in this endpoint's JSON response, and from there the GitHub Actions job log, for free. L2: `sweep-retention`'s result (`ingest-writer.ts`'s `RetentionResult`) is captured and its `expired` count passed straight through as `persistSignals`'s 4th arg — pure stats pass-through, no new logic here — so `counts.signals` ends up `{created, refreshed, suppressed, purged, expired}` | `runIngest`, `IngestResult` |
| `src/lib/server/tick.ts` | Pure freshness/lock decision logic for `/api/tick` (`isRecent`, injectable `now`, shared by both the 15min passive freshness threshold and the 10min lock threshold) plus its two queries: `getLastIngestAt` (`MAX(first_seen_at)`) and `tryAcquireLock` (atomic `settings.tick_lock` upsert — see Invariants). `selectFreshnessThreshold(manual)` picks between the passive 15min window and `MANUAL_FRESHNESS_THRESHOLD_MS` (an alias for `LOCK_THRESHOLD_MS` — the lock, not the freshness check, is the real abuse ceiling for a manual click) | `isRecent`, `selectFreshnessThreshold`, `getLastIngestAt`, `tryAcquireLock`, `FRESHNESS_THRESHOLD_MS`, `MANUAL_FRESHNESS_THRESHOLD_MS`, `LOCK_THRESHOLD_MS` |
| `src/lib/server/article-identity.ts` | Pure article identity hashing for de-duplication | `contentHash` |
| `src/lib/server/ingest-writer.ts` | Batched article persistence, cross-source dup-group linking, retention sweep. L2: `sweepRetention` now also expires moment-type 'new' signals (surge/first_seen/cross_category/sentiment) whose `last_evidence_at` is older than 7 days — both the type list and the day count are hardcoded product decisions, not settings; `novel_edge` is deliberately exempt (a novel connection stays until a human acts on it; the durable record lives in `entity_edges` regardless). Uses `make_interval(days => ...)` rather than parameter interpolation inside an `INTERVAL '...'` literal, which silently fails with this driver (see DEVLOG). Returns `RetentionResult` (`{expired}`) instead of `void`, threaded through by `run-ingest.ts` into the ingest response's `signals` stats | `persistArticles`, `sweepRetention`, `RetentionResult` |
| `src/lib/server/extract-v2.ts` | Pure candidate extraction: dictionary + compromise NLP + acronym + person-regex + product-pattern (mixed-case/digit product-name shapes, e.g. DeepSeek/GPT-5o) layers, normalization. `extractDictionaryOnlyCandidates` exposes just the dictionary layer for entity-ingest's LLM-path union. `TypeHint` is the full 15-type ontology (mirrors `entities_type_check`). `Candidate` carries optional `roleContext` and `prominence` (LLM-only judgment of how widely known an entity is — see `Prominence`); `CandidateLayer` adds `'llm'` (priority just under `'dictionary'`) and `'product-pattern'` (lowest priority); `addCandidate` carries both `roleContext` and `prominence` through the same layer-priority merge | `normalizeName`, `extractCandidates`, `extractDictionaryOnlyCandidates`, `addCandidate`, `Candidate`, `TypeHint`, `CandidateLayer`, `Prominence` |
| `src/lib/server/llm-extract.ts` | M-batch: LLM entity + relation extraction moved from live `/v1/messages` calls to the Anthropic Message Batches API (`POST/GET /v1/messages/batches`, plain `fetch`, no SDK), pinned to `claude-haiku-4-5-20251001` — 50% of live per-token pricing for an identical request. `submitBatch(sql, budgetUsd, chunks)` posts ONE request per ≤25-article chunk (`custom_id` = chunk index, `MAX_BATCH_SIZE`) in a single Batches API call; budget-gated against `llm_usage` (live + batch columns combined, `settings.llm_monthly_budget_usd`) before submitting. `pollBatch(batchId)` maps `processing_status` to `in_progress` (also `canceling`) / `ended` (with `resultsUrl`) / `failed` (with a reason). `fetchBatchResults(resultsUrl)` fetches and parses the JSONL results — one line per chunk, in ANY order, keyed by `custom_id` — reusing the unchanged defensive entity/relation parser (strips markdown wrapping; unrecognized entity type kept as `'other'`; unrecognized relation type or unlisted endpoint drops just that relation; missing/invalid prominence defaults to `'known'`) against each chunk's `message` field; a chunk's `result.type` of `'errored'`/`'canceled'`/`'expired'`, or an unparseable `message`, is reported per-chunk via `chunkFailures` rather than failing the whole batch. `recordBatchUsage` sums a batch's usage into `llm_usage`'s NEW `batch_input_tokens`/`batch_output_tokens` columns (migration 008), separate from the legacy `input_tokens`/`output_tokens` (August's already-recorded live spend keeps live pricing). `estimateCostUsd(input, output, batchInput=0, batchOutput=0)` prices live tokens at $1.00/$5.00 per Mtok and batch tokens at half that ($0.50/$2.50) — `getLlmMonthStats` sums both. Every submit/poll/results-fetch call is independently bounded by the same 12s `REQUEST_TIMEOUT_MS`, no retries (the next tick is the retry). Every failure path (budget, network, timeout, malformed response, non-2xx) returns a low-cardinality `{reason}` string rather than throwing; a non-2xx submit/poll/results-fetch also carries an optional `sample` — the response body reduced to one bounded, redacted diagnostic string (`<error.type>: <error.message>` when it parses as Anthropic's error shape, else a capped raw prefix; 200 chars max, URL-redacted via the same one-line regex as fame-sweep.ts's redactSecrets, copied not imported) — so reason keys stay low-cardinality while samples stay capped/redacted/few, since both reach a public repo's Actions log via the ingest JSON response. `callAnthropic` and the old single-call `extractEntitiesBatch` are deleted | `MODEL`, `MAX_BATCH_SIZE`, `REQUEST_TIMEOUT_MS`, `isLlmConfigured`, `submitBatch`, `pollBatch`, `fetchBatchResults`, `recordBatchUsage`, `getLlmMonthStats`, `estimateCostUsd`, `currentUtcMonth`, `ArticleInput`, `LlmMonthStats`, `ExtractedRelation`, `RelationType`, `ChunkExtractionResult`, `BatchResults`, `BatchPollResult` |
| `src/lib/server/entity-ingest.ts` | Resolves extracted candidates against the DB registry then static dictionaries, else queues as a candidate; batches all entity/mention/edge/candidate/relation writes. Tracks two clocks per mention — `effectiveAt` (news time: publish date) and `arrivalAt` (watch time: ingest arrival). M-batch: LLM extraction now spans two ticks via `migrations/008_llm_batches.sql`'s `llm_batches` table (invariant: at most one pending row). Each `processNewArticles` call: (1) `resolvePendingBatchStage` — no row: no-op; row + no API key: heuristic-process its articles immediately (can't poll without a key) and delete the row; row younger than 6h: poll it — `ended` applies each chunk's results through the SAME parser/merge-with-dictionary path live calls used (`applyBatchResults`, keyed by chunk index over the row's stored `article_ids` order, NOT by re-querying — an id absent from the re-query, per the `entities_processed_at IS NULL` exactly-once guard, is silently skipped), an `errored`/`canceled`/`expired`/malformed chunk or a results-fetch failure heuristic-processes that chunk's/the whole batch's articles instead, row deleted either way; row 6h+ old: abandoned, heuristic-processed without polling (no `cancel` call — the exactly-once guard makes late results harmless), row deleted. (2) only when no batch remains pending: selects fresh unprocessed heads (`selectUnprocessedHeads`, now excludes a still-pending batch's own article ids), budget-gates, and submits ONE new batch (`submitNewBatchStage`) — those heads are NOT processed this tick (results arrive next tick); a submit failure heuristic-processes them immediately instead, same as a live-call failure used to. `LLM_WAVE_SIZE`/`LLM_TIME_BUDGET_MS`/wave-dispatch machinery deleted — one submit/poll/results-fetch call per tick, each independently bounded, replaces the wall-clock multi-wave budget. `EntityIngestStats.llm` gains `batch: {submittedArticles, retrievedArticles, pendingAgeMinutes}` and `failureReasons` (low-cardinality reason -> count, covering every non-success path this tick), plus an optional `errorSamples` (up to 2 DISTINCT sanitized `<error.type>: <error.message>` samples from a submit/poll/results-fetch non-2xx body — see llm-extract.ts's `sampleErrorBody` — field OMITTED when there are none, mirroring fame-sweep.ts's `exceptionSamples`). A third auto-track path sits alongside the dictionary first-hit rule: an unresolved LLM candidate with `prominence === 'famous'` is also inserted straight into `entities` (status `tracked`) instead of `entity_candidates` — see Invariants. `entity_candidates` rows also accumulate `contexts` (LLM role phrases, deduped, capped at 3) and `co_entities` (canonical names of tracked entities resolved in the same article, deduped, capped at 5). Relations resolve against the pre-run registry overlaid with this run's fresh inserts (dictionary and famous-auto-accept alike); an unresolved endpoint or a resolved self-loop drops the relation for this run | `processNewArticles`, `dedupeMentions`, `rollupHourlyMentions`, `rollupEntityEdges`, `rollupRelations`, `rollupCandidate`, `chunkArticles`, `EntityIngestStats` |
| `src/lib/server/settings.ts` | Tunable detector/brief thresholds: DB-overlaid defaults, type-validated per key | `DEFAULTS` (incl. `llm_monthly_budget_usd: 5`), `getSettings`, `Settings` |
| `src/lib/server/fame.ts` | L2A shared fame test, used by both `developments.ts` and `detectors.ts`: an entity/candidate is famous when its name/aliases hit a seeded COUNTRY/ORG/REGION dictionary term (`isDictionaryFamous`), OR its lifetime distinct-source-name breadth is >=12 (`isBreadthFamous`; `loadLifetimeSourceBreadth` — no time window, no `dup_group_id` filter, bounded in practice by the retention sweep), OR its baseline clears the population's own top quartile (`isVolumeFamous`/`computeFameVolumeThreshold` — p75, same percentile math and population as `developments.ts`'s anchor threshold, floored at 3, but `Infinity` for an empty population and never true for `baselineDaily<=0`), OR the persisted Wikidata verdict says so. Deliberately broader than `isAnchor`: anchors provide context, but famous entities must never be card subjects or unsuppressed novel-edge endpoints. `loadLifetimeSourceBreadth` is only ever called with a small, bounded entity-id set — never the whole tracked roster. M006 fame layer: `computeWikiFameVerdict({matched, sitelinks, medianMonthlyPageviews})` is the pure Wikidata/Wikipedia verdict — famous when `medianMonthlyPageviews >= FAME_PAGEVIEWS_THRESHOLD` (20,000), OR `sitelinks >= FAME_SITELINKS_THRESHOLD` (25) AND `medianMonthlyPageviews >= FAME_SITELINKS_CORROBORATION_PAGEVIEWS` (3,000) — the sitelinks prong deliberately requires pageview corroboration, since Wikipedia sitelinks grow within days of a breaking story and alone are not spike-immune; no Wikidata match at all is not_famous. `FameFacts` gains `storedFame` (the persisted `entities.fame` column — `StoredFame`, `'unknown'\|'not_famous'\|'famous'`), and `isFamous` ORs in `storedFame === 'famous'`: a stored `'not_famous'` or `'unknown'` never vetoes a heuristic prong that already fires — the stored verdict only ever tightens the existing heuristics, never loosens them | `isFamous`, `isDictionaryFamous`, `isBreadthFamous`, `isVolumeFamous`, `computeFameVolumeThreshold`, `loadLifetimeSourceBreadth`, `FameFacts`, `computeWikiFameVerdict`, `WikiFameInput`, `StoredFame`, `FAME_PAGEVIEWS_THRESHOLD`, `FAME_SITELINKS_THRESHOLD`, `FAME_SITELINKS_CORROBORATION_PAGEVIEWS` |
| `src/lib/server/detectors.ts` | Five pure scoring functions (rate surprise, first-seen novelty, novel entity edge, category-spread deviation, sentiment-delta deviation) plus the batched-query orchestrator; every candidate's evidence has a numeric score decomposition + capped articleIds. `scoreCategorySpread`/`scoreSentimentDelta` fire on a deviation from the entity's OWN trailing-14-day baseline (excluding the last 24h), not an absolute level — a prominent entity spanning many categories, or one whose coverage is always negative, does not perpetually fire. All five detectors (not just the three originally history-dependent ones) gate on the system-wide warm-up epoch, since the two baseline-driven detectors need real history too. `detectNovelEdges` additionally suppresses a candidate when BOTH endpoints are famous (`fame.ts`'s `isFamous`, full test — one-famous and zero-famous edges still fire): `runDetectors` now loads the hourly-agg panel before its main `Promise.all` (a slight restructure) so that panel's baselineDaily values (the fame volume-threshold population) are ready for the novel-edge pass, which also loads aliases (added to `loadRecentNovelEdges`'s own query) and lifetime breadth (`fame.ts`'s `loadLifetimeSourceBreadth`, bounded to just that run's edge endpoints) for its own small endpoint set. M006: `loadRecentNovelEdges` also selects both endpoints' stored `fame` column (`fame_a`/`fame_b`); `suppressFamousEdges`'s `endpointIsFamous` threads each endpoint's `storedFame` into `isFamous` alongside its heuristic facts, so a stored-famous endpoint counts as famous for the both-famous suppression even with every heuristic prong negative. L2A: the same fame gate now also applies to the four single-entity detectors (surge/first_seen/cross_category/sentiment) — `suppressFamousSubjects` batches fame facts (`loadSubjectFameFacts`) + lifetime breadth for the union of this run's candidate subjects in one pass and drops any famous one, reusing `computeFameThreshold` (renamed from `computeNovelEdgeFameThreshold`, now shared by both gates) and `fame.ts`'s `isFamous`/`loadLifetimeSourceBreadth` rather than a second fame implementation; `novel_edge`'s own famous-famous rule is unchanged. `loadFameBaselinePanel` is exported so `signal-store.ts`'s stock-side purge can apply the identical fame test to existing signal rows without re-deriving the baseline panel/volume threshold | `runDetectors`, `scoreSurge`, `scoreFirstSeenNovelty`, `scoreNovelEdge`, `scoreCategorySpread`, `scoreSentimentDelta`, `isBootstrapCohort`, `computeWarmupState`, `computeEffectiveBaselineDays`, `CandidateSignal`, `loadFameBaselinePanel`, `FameBaselinePanel` |
| `src/lib/server/signal-store.ts` | Signal persistence: dismiss-cooldown-aware upsert against the active partial unique index, lifecycle transitions, the shared severity-ordered/entity-and-evidence-article-resolved query used by both signal routes. L2: `persistSignals` now also runs `purgeFamousSignals` on every call (even with an empty candidate list) — deletes `state='new'` rows that today's fame gate (`detectors.ts`'s `loadFameBaselinePanel` + `fame.ts`'s `isFamous`) would have blocked: a single-entity type whose subject is famous, or a `novel_edge` row whose endpoints are BOTH famous. Never touches `seen`/`promoted`/`dismissed` rows. `persistSignals` also takes an optional `expiredCount` (4th arg, default 0) that `run-ingest.ts` passes straight through from `ingest-writer.ts`'s retention sweep, so `PersistResult` — now `{created, refreshed, suppressed, purged, expired}` — is the single place both cleanup counts surface in the ingest response, without a second cross-module call `route.ts`'s test mocks would need to know about | `persistSignals`, `transitionSignal`, `loadSignals`, `SignalJson`, `EvidenceArticle`, `SignalAction`, `PersistResult` |
| `src/lib/server/wikidata.ts` | The only module that talks to Wikimedia — plain `fetch`, no SDK, mirroring `llm-extract.ts`'s per-call `AbortController` timeout (4s here) and never-throws-to-caller contract. `lookupWikidataFame(name, now?)`: `wbsearchentities` (English, top 5 hits — M008, was top 1) → if none, a clean `{ok:true, lookup:{matched:false,...}}` (not a failure) → ONE `wbgetentities` call for all 5 candidate ids → M008 `pickBestCandidate` picks the candidate with the highest total sitelink count among those with an `enwiki` sitelink (ties favor the earlier/more-relevant search result; if none has one, keeps the pre-M008 top-hit-only behavior) instead of blindly trusting the first search result — fixes a real production false match (entity "ICC" resolved to a 2-sitelink Antwerp arts venue instead of the International Criminal Court) — then, if the winning candidate has an `enwiki` sitelink, the Wikimedia pageviews REST API, monthly granularity, for `computeWindowMonths`'s 12 full calendar months ending 2 months before `now` (keeps the current news spike, and processing lag, out of the window), via the shared `lookupFameFromMatch`. `computeMedianPageviews` is the spike-immunity linchpin: a window month with no data (article too young, or a legitimate pageviews-API 404 for "no data in range") reads as 0, and the median is always taken over all 12 months — a 3-month-old article with huge recent views still produces a low median. M008: `resolveNamesBatch(names)` — ONE `wbgetentities` call (`sites=enwiki&titles=<up to 50 pipe-joined names>&normalize=1`) resolving every name whose text exactly (case/underscore-insensitively) matches an English Wikipedia title, e.g. "Ukraine"; a name whose real article title differs (e.g. "Putin" → "Vladimir Putin") is simply absent from the result and must fall back to `lookupWikidataFame`'s per-name path — `fame-sweep.ts` calls this first for its whole batch. M008: every failure reason is now specific and machine-readable, including the HTTP status where one exists (`fetchFailureReason`, private) — e.g. `search_http_403`, `search_http_429`, `search_timeout`, `entities_malformed_body`, `batch_entities_http_500` — replacing four generic strings that gave zero diagnostic signal during a real 24h 100%-failure production incident (see DEVLOG). M008: `USER_AGENT` now also carries a contact address per Wikimedia's User-Agent policy — the project's public GitHub repo URL, not an email (a deliberate operator choice: the policy accepts a contact page, avoiding a personal address in public source). Every pure parser (`parseSearchResponse`, `parseSearchResponseIds`, `parseEntitiesResponse`, `parseEntitiesResponseMulti`, `parseBatchEntitiesResponse`, `parsePageviewsResponse`) is exported standalone for tests; network is isolated behind one `fetchJson` wrapper | `lookupWikidataFame`, `lookupFameFromMatch`, `resolveNamesBatch`, `computeWindowMonths`, `computeMedianPageviews`, `parseSearchResponse`, `parseSearchResponseIds`, `parseEntitiesResponse`, `parseEntitiesResponseMulti`, `parseBatchEntitiesResponse`, `parsePageviewsResponse`, `ALIAS_LANGUAGES`, `USER_AGENT`, `REQUEST_TIMEOUT_MS`, `WikidataLookup`, `WikidataLookupResult`, `EntityDetails`, `NameBatchResult` |
| `src/lib/server/fame-sweep.ts` | Per-tick background fame sweep. `selectSweepBatch` (private): tracked entities needing a (re)check — `fame='unknown'` never checked or the last attempt failed (a transient lookup failure), retried after `UNKNOWN_RETRY_HOURS` (6h, via `make_interval(hours => ...)`) — frequent enough to converge a transient-failure pool in about a day, infrequent enough to stay polite to Wikimedia for an entity that keeps failing; OR `fame='not_famous'` checked >30 days ago (a rising entity can graduate); `fame='famous'` is permanent and never reselected. Never-checked entities sort first, then newest `first_seen_at`; capped at 40. M007: every branch additionally excludes `fame_locked = true` — a human verdict set via `PATCH /api/entities/[id]` must never be silently overwritten by the automated sweep. `checkAndWriteEntityFame` (shared by `runFameSweep`'s loop and the candidates route's inline post-accept check): one entity's lookup — M008: an optional `batchMatch` (`wikidata.ts`'s `EntityDetails`) skips straight to `lookupFameFromMatch` (pageviews only) when `runFameSweep`'s upfront batch resolution already matched this entity's name, otherwise the full `lookupWikidataFame` per-name path — on success writes `fame` (via `fame.ts`'s `computeWikiFameVerdict`), `wiki_title`/`wiki_sitelinks` (stored whenever a match occurred, regardless of verdict), `wiki_pageviews_monthly`, `fame_checked_at`; on a RETURNED failure writes ONLY `fame_checked_at` (fame stays whatever it was, and the retry queue rotates instead of head-of-line blocking) and now returns a `FameCheckOutcome` (M008, was a bare `"success"|"failure"` string) carrying the specific failure reason for the tally and circuit breaker below. The whole lookup+write body runs inside a try/catch: anything that THROWS instead (a transient DB-query failure, response parsing, the verdict, the alias merge, or a write error) is caught, logged with one greppable `console.error` line (`entity id`, `JSON.stringify`'d canonical name, and the error), tagged via M009's `describeException` (pure, exported) with an `exception_<ErrorName>` reason plus one sanitized sample message (never a rate-limit signal), and degrades identically via a minimal rescue write (`writeRescueStamp`, private) that stamps ONLY `fame_checked_at`. Only `writeRescueStamp` itself throwing propagates out uncaught. `mergeAliases` (pure): existing ∪ Wikidata labels/aliases ∪ seeded-dictionary aliases where a `DictEntry.name` matches `canonical_name` — deduped via `normalizeName`, existing aliases never dropped, new ones capped so the total never exceeds 24. M008: `runFameSweep` first calls `wikidata.ts`'s `resolveNamesBatch` ONCE for the whole selected batch's canonical names (up to 40, under Wikidata's 50-id/title ceiling) — a name it resolves skips straight to pageviews for that entity; a non-rate-limit batch failure (timeout, 500, malformed body) just means nobody gets the shortcut this tick, everyone falls back to the per-name path as before. `FameSweepStats` gains `failureReasons` (`Record<reason, count>`, always present, tallied in `tallyOutcomes`) — the diagnostic this module lacked during a real 24h 100%-failure incident where every lookup failed for an unknown reason and nothing surfaced it (see DEVLOG) — and an optional `abortedReason` (currently always `"rate_limited"`). M009: the reason keys for a thrown exception are no longer the single opaque `"unexpected_exception"` — `describeException` keys them on the error's own class name (low cardinality, so 34 identical crashes stay one tally row) and returns one sanitized sample message, and `FameSweepStats` gains an optional `exceptionSamples` (up to 3 DISTINCT sample strings, present only when something actually threw). Every sample is passed through `redactSecrets` (private) first — a Neon driver error can quote `DATABASE_URL` verbatim, and these samples travel into the ingest JSON response and from there a PUBLIC repository's Actions log, so every URL-shaped substring becomes `<redacted-url>` and the message is capped at 160 chars. This exists because the plain `"unexpected_exception"` tag proved useless in a real incident: 34 of 40 lookups threw, the tag said only that they threw, and the `console.error` line carrying the actual cause expired with Vercel's one-hour log retention before anyone read it. Circuit breaker (`isRateLimitReason`, private): if the batch call OR any per-entity lookup in a run returns HTTP 429 or 403, the run aborts immediately — a batch-level rate limit returns before touching any entity; a per-entity one is checked after each chunk finishes (never pre-empting an in-flight chunk, same convention as the wall-clock deadline). Entities in chunks never reached are simply never passed to `checkAndWriteEntityFame`, so their `fame_checked_at` is left completely untouched and they retry on the normal schedule — continuing to hammer a service that just refused us both sustains the block and is impolite; the next run (~30 min away) tries again clean. `runFameSweep` still processes the batch CHUNK_SIZE (3) entities at a time, each chunk running concurrently via `Promise.all`, within a ~12s wall-clock budget checked between chunks (the upfront batch call isn't itself deadline-gated, same unaccounted-for-but-bounded treatment as `selectSweepBatch`'s own query time, pushing the absolute worst case to ~28s, still inside Vercel's 60s ceiling) | `runFameSweep`, `checkAndWriteEntityFame`, `describeException`, `mergeAliases`, `FameSweepStats`, `FameCheckEntity`, `FameCheckOutcome` |
| `src/lib/server/entity-admin.ts` | EM-1 server foundation for the (future) Entity Manager screen. `toEntityAdminJson` (shared row shaping, reused by `searchEntities` and the `PATCH /api/entities/[id]` handler): every row carries its fame evidence fields (`wikiTitle`, `wikiSitelinks`, `wikiPageviewsMonthly`, `fameCheckedAt`) alongside `fame`/`fameLocked` — `fameCheckedAt=null` is how a never-checked entity stays distinguishable from a real (if inconclusive) verdict. `searchEntities(sql, {q, status, fame, limit, offset})`: case-insensitive match on `canonical_name` OR any alias via `extract-v2.ts`'s `normalizeName` (the SAME scheme ingest resolution uses) — `status`/`fame` are pushed down to SQL as cheap exact filters, `q` is matched in JS (porting `normalizeName`'s NFKD-fold/corporate-suffix-stripping into SQL would risk the two schemes drifting apart), bounded/paged (default limit 50, max 200) with every input validated at the boundary — a malformed limit/offset/status/fame throws `ValidationError` rather than being coerced, and the route maps that to 400. Phase 3a: also validates `sort` (`name` asc | `first_seen`/`last_seen`/`activity` desc, default `last_seen` — null last-seen always sorts after real timestamps), `fameChecked` (`never|checked` — `checked` + `fame=unknown` is exactly the stats "parked" definition, so no separate parked param exists), `fameLocked` (`"true"` only); fameChecked/fameLocked filter in JS alongside `q` over the status/fame-filtered SQL rows. Each returned row is enriched to `EntityListItemJson` (`lastSeenAt`, `mentions7d`, `sources7d`) via `loadMentionsInWindow`/`loadSourcesInWindow` — matched-set-wide GROUP BY queries (mentions from `entity_mentions_hourly`; sources as COUNT(DISTINCT source_name) via article_entities/articles, never summed hourly source_counts, which would double-count), zero-filled, computed before the offset/limit slice. Phase 3a follow-up: `classifyEntityRole` (pure) + `attachEntityRoles(sql, rows)` compute `role`/`roleReasons` (`anchor`/`famous`/`satellite`) for the returned page only (list) or the single entity (detail) — one `loadEntityBaselinePanel` call per request (imported from developments.ts) feeding both `computeAnchorThreshold`/`computeFameVolumeThreshold` in JS from the same population `getDevelopmentsDetailed` scans, plus one `loadLifetimeSourceBreadth` query bounded to those rows' ids; reasons are the identical prong checks `isAnchor`/`isFamous` themselves OR together (decomposed for the reason list, with the composite functions still the actual gate), never a re-tuned threshold. `loadEntityStats(sql)`: one-query health readout (`COUNT(*) FILTER (WHERE ...)` conditional aggregates, no GROUP BY) scoped to `status='tracked'` — counts by fame, "parked" (`fame='unknown' AND fame_checked_at IS NOT NULL`, a real but inconclusive verdict) counted separately from "never checked" (`fame_checked_at IS NULL`), locked count, total tracked, oldest/newest `fame_checked_at`; an empty table still returns one all-zero/null row. `findSimilarNames(entities)`: PURE, DB-free duplicate-name grouping for a later merge-suggestion UI — two rules only, (1) identical after `normalizeName` + punctuation/period stripping, or (2) one name a whole-word prefix OR suffix of the other ("Air Force"/"Air Force One", "Modi"/"Narendra Modi") — no fuzzy distance metric; grouped via union-find over pairwise matches so the result is order-independent; only suggests, never merges | `searchEntities`, `loadEntityStats`, `findSimilarNames`, `toEntityAdminJson`, `loadMentionsInWindow`, `loadSourcesInWindow`, `classifyEntityRole`, `attachEntityRoles`, `EntityRole`, `EntityRoleResult`, `ClassifyEntityRoleInput`, `ValidationError`, `SearchEntitiesParams`, `SearchEntitiesResult`, `EntityStats`, `EntityAdminJson`, `EntityListItemJson`, `EntitySort`, `SimilarNameEntity`, `SimilarNameGroup` |
| `src/lib/server/brief.ts` | Daily-brief assembly: pure recency-decayed story ranking plus the top-stories/new-entities queries. `movers`: top 5 tracked entities by `lift = observed24h / max(baselineDaily, 0.5)`, requiring `observed24h >= 3`; reuses `detectors.ts`'s exported `computeWarmupState`/`computeEffectiveBaselineDays` (imports only — no changes to that file) and duplicates its own tiny system-epoch query rather than importing a private detector helper. `warmup` is returned empty-movers during warm-up. M006: `loadNewEntities` excludes a stored `fame = 'famous'` entity (`unknown`/`not_famous` still appear) — Brief's New Entities strip is names the reader does NOT already know, and a famous auto-accept (Facebook, Schwarzenegger) is noise there | `getBrief`, `computeStoryScore`, `computeLift`, `Brief`, `TopStoryJson`, `NewEntityJson`, `MoverJson` |
| `src/lib/server/developments.ts` | Read-only, request-time-computed "Developments" cards: four independent card sources — R (stated `entity_relations`), N (newly tracked satellite + co-occurring anchors), C (corroborated `entity_candidates`), E (co-coverage `entity_edges`) — each producing a `CardDraft`, then a uniform `finalizeCards` pipeline (eligibility -> score -> cross-source dedupe -> sort/cap at 8). Every draft carries a subject-fame verdict (`fame.ts`'s `isFamous` for R/N/E tracked-entity subjects; dictionary-only, on both `display_name` and `name_norm`, for C's untracked candidates) alongside the pre-existing `isAnchor` verdict; `passesEligibility` rejects a famous OR anchor subject uniformly across all four sources. Source R's pairing rule is `isSatellite` (NOT famous OR anchor) applied to BOTH relation endpoints — a card exists only when exactly one endpoint is a satellite (the subject; the other is context), deliberately widening R's context pool to famous-but-modest-volume entities that previously anchored nothing. E and N keep their pre-existing `isAnchor`-only pairing/context resolution, NOT widened — a famous E/N subject is instead caught by the uniform eligibility gate. N additionally skips an "instant ubiquity" entity (`isInstantUbiquity`: >=6 distinct sources within its first 48h of tracking, both timestamps watch-time — breaking mega-news about an already-famous subject, not an emerging satellite). C's `isCorroboratedCandidate` qualifies via the original persistence bar (day_count>=2, >=2 sources) OR a same-day "breaking satellite" shortcut (>=3 sources, any day_count). `getDevelopmentsDetailed(sql, now?, {cap?})` is the underlying implementation — returns `{cards, diagnostics}` where `diagnostics = {draftCount, eligibleCount, rejected}` tallies rejected drafts by first-failing eligibility reason via `classifyDrafts`, and `cap` overrides the default `CARD_CAP` (8; fail-fast on a non-positive-integer cap); `getDevelopments` is a thin delegate discarding diagnostics, signature/behavior unchanged for `brief.ts`. `passesEligibility` returns the first-failing `EligibilityReason` (`anchor_subject|famous_subject|no_evidence|single_source|no_anchor`) or `null` when eligible. `getDevelopments` returns `[]` before any operating history exists. M006: `loadEntityBaselinePanel` also selects `e.fame`; `TrackedEntityMeta`/`EntityBaselineRow` carry it through as `StoredFame`; `computeIsFamous` (R/E) and source N's inline `isFamous` call both thread `storedFame` into the shared fame test, so a stored-famous, heuristically-invisible subject is excluded the same as one caught by a heuristic prong. Source C (untracked candidates, no `fame` column) is unchanged — still dictionary-only | `getDevelopments`, `isAnchor`, `isSatellite`, `isInstantUbiquity`, `computeAnchorThreshold`, `rankAnchors`, `passesEligibility`, `isCorroboratedCandidate`, `computeNovelty`, `computeCorroboration`, `computePersistence`, `computeRelationStrength`, `computeAnchorContext`, `computePenalty`, `scoreDevelopment`, `countDistinctSources`, `countDistinctUtcDays`, `computeStaleReporting`, `buildWhyShown`, `capCandidateFanout`, `resolveAmbiguousTitles`, `selectDisplayEvidence`, `dedupeCards`, `sortAndCapCards`, `DevelopmentCardJson`, `getDevelopmentsDetailed`, `DevelopmentDiagnostics`, `DevelopmentsResult`, `GetDevelopmentsOptions`, `EligibilityReason`, `loadEntityBaselinePanel`, `EntityBaselineRow` |
| `migrations/001_core.sql` | Schema v1: articles, entities, article_entities, entity_mentions_hourly, entity_edges, entity_candidates, signals, settings | — |
| `migrations/002_entity_indexes.sql` | `article_entities_entity_idx` (entity_id-first index for entity→articles lookups), `articles.entities_processed_at` (idempotent entity-pass marker column, replaces a `NOT EXISTS(article_entities)` gate), `entity_edges_entity_b_idx` (entity_b-first index for entity→edges lookups) — not yet run against the production database | — |
| `migrations/003_time_semantics_repair.sql` | One-time data repair for the news-time/watch-time conflation: recomputes `entities.first_seen_at`/`last_seen_at` from `MIN`/`MAX(articles.first_seen_at)` via `article_entities`; clamps `entity_edges.first_seen_at`/`last_seen_at` up to `MIN(articles.first_seen_at)`; `DELETE FROM signals` (every pre-fix row is an artifact of the miscalibrated engine). No-ops on an empty database — not yet run against the production database | — |
| `migrations/004_llm_and_candidate_context.sql` | `llm_usage` (month-keyed token/call ledger for the LLM budget cap); `entity_candidates.contexts`/`co_entities` (`TEXT[] NOT NULL DEFAULT '{}'`) — not yet run against the production database | — |
| `migrations/005_ontology_and_relations.sql` | Re-adds `entities_type_check` with the 15-type ontology (every pre-existing production value stays valid, no data migration); adds `entity_relations` (directed, typed, evidence-linked — `source_id`/`target_id`/`relation` PK, `CHECK (source_id <> target_id)`, FK-cascade on both entity ids, `target_id` index) — not yet run against the production database | — |
| `migrations/006_fame_layer.sql` | Adds `entities.fame` (`TEXT NOT NULL DEFAULT 'unknown'`, `CHECK IN ('unknown','not_famous','famous')`), `wiki_title`, `wiki_sitelinks`, `wiki_pageviews_monthly`, `fame_checked_at` (all nullable) — fully additive, every existing row defaults to `fame='unknown'`, identical to a brand-new never-checked entity — not yet run against the production database | — |
| `migrations/007_fame_override.sql` | Adds `entities.fame_locked` (`BOOLEAN NOT NULL DEFAULT false`) — fully additive, every existing row defaults to unlocked (identical to the sweep's prior always-eligible behavior). `fame-sweep.ts`'s `selectSweepBatch` excludes locked rows from every branch; `PATCH /api/entities/[id]` sets it automatically when `fame` is set, with an explicit `{fameLocked: false}` release escape — not yet run against the production database; **the operator must apply it manually before merge** | — |
| `migrations/008_llm_batches.sql` | M-batch: adds `llm_batches` (`batch_id TEXT PRIMARY KEY`, `submitted_at TIMESTAMPTZ NOT NULL`, `article_ids BIGINT[] NOT NULL`) — the single pending-batch row `entity-ingest.ts` polls/applies each tick (invariant: at most one row, enforced in code, not by this schema); adds `llm_usage.batch_input_tokens`/`batch_output_tokens` (`BIGINT NOT NULL DEFAULT 0`), fully additive, priced at half the existing live-call rate — not yet run against the production database; **the operator must apply it manually before merge**. Deliberately has NO `INSERT INTO schema_migrations` statement of its own, matching 001-007's actual convention (`scripts/migrate.mjs` appends that insert itself, in the same transaction) — the integration test helper (`__tests__/helpers/pg-sql.ts`) applies every migration file's raw statements against a schema with no `schema_migrations` table at all, so such an INSERT inside the file would break every integration test on a fresh schema | — |
| `scripts/migrate.mjs` | Idempotent migration runner (`schema_migrations` ledger) | — |
| `src/app/page.tsx` | Home page — renders the main dashboard | `Home` component |
| `src/app/layout.tsx` | Root HTML layout with metadata and font loading | `RootLayout` component |
| `src/app/globals.css` | Tailwind directives and dark-scrollbar styles | — |
| `src/components/DashboardTable.tsx` | Thin composition shell: imports hook + sub-components, composes layout. Live-feed loading/empty states are gated to items-dependent tabs only (excludes Brief/Signals/Entities, which are DB-backed — EM-2a added Entities to this exemption list; see MANIFEST's TabContent.tsx row for why the two live in separate files). EM-2b: passes `onEntitySelect={setPanelEntityId}` to `TabContent` so `EntitiesTab`'s row name-click can open the existing global `EntityPanel` directly by id (no name-resolution round trip); `isItemsDependentTab` drops the retired `"review"` `TabKey` | `DashboardTable` |
| `src/components/dashboard/EntityFilterBanner.tsx` | Entity filter notification bar with clear controls | `EntityFilterBanner` |
| `src/components/dashboard/FeedTable.tsx` | Desktop sortable table with header and row sub-components. Rows are neutral (alternating background only, no category-derived severity) with a quiet uniform category chip; a muted "+K" chip (K = `clusterSize - 1`) appears next to the source name when `clusterSize >= 2` | `FeedTable` |
| `src/components/dashboard/FeedCardList.tsx` | Mobile responsive card layout for feed items. Cards are neutral (no category-derived severity) with a quiet uniform category chip; a muted "+K" chip (K = `clusterSize - 1`) appears next to the source name when `clusterSize >= 2` | `FeedCardList` |
| `src/components/dashboard/TabContent.tsx` | Lazy-loaded tab switcher for Brief, Feeds, Network, Map, Signals, Entities. Brief/Signals/Entities are DB-backed and render before the `items.length === 0` gate below them, independent of the live feed array — a tab that renders here without joining that exemption returns `null` on an empty feed (a known trap: this happened once already, see DEVLOG 2026-08). `DashboardTable.tsx` keeps a second, independent copy of the same items-dependent judgment (for its own loading/empty overlays) — a new DB-backed tab must join both. EM-2b: the Review tab and its dynamic import are gone (absorbed into Entities' candidate-queue segment); `onCandidatesChanged` now routes to `EntitiesTab` instead of the deleted `ReviewTab` (same callback, re-pointed — see DEVLOG), and a new `onEntitySelect(id)` prop is passed through to `EntitiesTab` only, for its evidence-panel row clicks | `TabContent` |
| `src/components/dashboard/index.ts` | Barrel export for dashboard sub-components | — |
| `src/components/HeaderBar.tsx` | Top navigation with search, category filter, theme toggle, tab switcher (Brief first; EM-2a added Entities as the last tab), ingest-mode badge. Refresh button reflects the optional `refreshState` prop: disabled with "Collecting… ~1 min" while `'collecting'`, a brief "Up to date" while `'fresh'`, otherwise "Refresh". EM-2b: the Review tab is gone; its pending-count badge moved onto the Entities tab label ("Entities (N)"), same `candidateCount` prop, same source — `TabKey` no longer includes `"review"` | `HeaderBar` |
| `src/components/BriefTab.tsx` | Thin composition shell: imports hook + sub-components, composes layout — the default landing tab. Renders a "Generated Xm ago" line ("Generated just now" inside `timeAgo`'s 5s "NOW" window; full timestamp in `title`; omitted entirely when `generatedAt` is missing/unparsable — never "NaN ago"); `isEmpty` checks all five sections (`developments`, `movers`, `signals`, `newEntities`, `topStories`) — previously ignored `developments`/`movers`, so a brief containing only those falsely rendered "All quiet" | `BriefTab` |
| `src/components/brief/BriefSignalsSection.tsx` | Active signals grouped by severity, using the shared `ManagedSignalCard` (Seen/Promote/Dismiss actions); within each severity group, `state === "promoted"` signals sort first (stable otherwise) and get a "watching" chip rendered by this section wrapping the card — `ManagedSignalCard` itself untouched | `BriefSignalsSection` |
| `src/components/brief/BriefNewEntitiesSection.tsx` | Chip row of newly-tracked entities with type (underscores→spaces) and source counts | `BriefNewEntitiesSection` |
| `src/components/brief/BriefMoversSection.tsx` | "Unusually active" chip strip of top-5 lift movers ("Entity ×4.2 · 17 today vs ~4.3/day" — baseline to one decimal below 10, whole number at/above), each opening the entity panel; hidden when empty or during warm-up (`BriefDevelopmentsSection` owns the one warm-up line) | `BriefMoversSection` |
| `src/components/brief/BriefDevelopmentsSection.tsx` | Development cards in a grid; header shows the card count ("Developments · 8") plus a "Ranked by score" subtitle when non-empty; owns the one warm-up line and the honest zero-card message; each card has an evidence expander and a hover score-parts breakdown | `BriefDevelopmentsSection` |
| `src/components/brief/BriefTopStoriesSection.tsx` | Ranked top-stories list: headline link, cluster size, source count, age | `BriefTopStoriesSection` |
| `src/components/brief/index.ts` | Barrel export for brief sub-components | — |
| `src/components/EntityPanel.tsx` | Slide-over entity timeline: profile, inline-SVG hourly sparkline, recent articles, related-entity (co-occurrence) chips. EM-2b: gains a "Relations" section rendering the typed/directed `relations.incoming`/`outgoing` `GET /api/entities/[id]` already returned but this component never read (`EntityDetail` widened by one field) — each line reuses the existing `onSelectRelated` callback, so no new prop was needed to reuse this component from `EntitiesTab` | `EntityPanel` |
| `src/components/EntitiesTab.tsx` | EM-2a: thin composition shell (mirrors SignalsTab/BriefTab) — imports `useEntitiesTab` + `entities/` sub-components, owns loading/error/db-unconfigured/empty states. "No entities match" (a filter — `q`/`status`/`fame` off their neutral defaults, i.e. `status !== "tracked"` — narrowed the result to zero) is distinguished from "No entities tracked yet" (a genuinely empty table). EM-2b: absorbs the deleted ReviewTab — a `SegmentedControl` ("Tracked" / "Review queue (N)") toggles between the existing tracked table (`TrackedView`) and a new candidate-queue view (`QueueView`, backed by `useCandidateQueue`); both hooks load unconditionally on mount regardless of which segment is active, so the nav badge count and the queue itself are never stale just because the user hasn't clicked into that segment. Takes `onEntitySelect(id)` (row name-click -> the global `EntityPanel`, Part B) and `onCandidatesChanged(count)` (badge sync, Part D) as props from `TabContent`. An `ErrorToast` per segment composes that segment's own error state (`[error, actionError]` for tracked; `[error, actionError, ...bulkErrors]` for the queue) — one dismiss clears everything currently shown. Usability pass: also renders a `SuccessToast` per segment (`tab.toast`/`queue.toast`) above the `ErrorToast`, and passes `status`/`fame`/`setStatus`/`setFame` into `StatsStrip` so its tiles double as filters, and `pageSize`/`setPageSize` into `EntitiesTable` | `EntitiesTab` |
| `src/components/entities/types.ts` | EM-2a: client-side DTOs mirroring `entity-admin.ts`'s `EntityAdminJson`/`EntityStats` — `EntityRowData`, `EntityStats`, `EntityPatch`, `StatusFilter`/`STATUS_FILTERS` (`tracked`/`dismissed`/`all`), `FameFilter`/`FAME_FILTERS` (`all`/`famous`/`not_famous`/`unknown` — display-renamed to Established/Emerging/Unchecked at every render site, values unchanged), `ENTITY_TYPES` (the 15-type ontology). EM-2b: `ReviewTab.tsx`'s own duplicate of this same ontology is gone (deleted with that file) — its candidate-queue successor imports `ENTITY_TYPES` from here instead; adds `CandidateRowData` (mirrors `/api/candidates` GET's row shape byte-for-byte) and `BulkItemResult` (mirrors `/api/candidates/bulk`'s per-item response shape). Usability pass: adds `ToastState` (`{message, onUndo?}` — consequence narration's shared shape between `SuccessToast` and both tab hooks) and `PAGE_SIZE_OPTIONS`/`PageSizeOption` (`50\|100\|250`, shared between `useEntitiesTab` and `PaginationFooter`'s opt-in rows-per-page control) | `EntityRowData`, `EntityStats`, `EntityPatch`, `StatusFilter`, `FameFilter`, `ENTITY_TYPES`, `CandidateRowData`, `BulkItemResult`, `ToastState`, `PageSizeOption`, `PAGE_SIZE_OPTIONS` |
| `src/components/entities/StatsStrip.tsx` | EM-2a: seven plain number+label tiles from `view=stats` — tracked, famous, not famous, unknown, parked, never checked, locked. Usability pass: Tracked/Established/Emerging/Unchecked are now `<button>`s (`aria-pressed` for the active one) wired to `status`/`fame`/`setStatus`/`setFame` — clicking applies that filter, clicking the already-active one clears it; Parked/Never checked/Locked stay static `<div>`s (no API query param exists for "checked but inconclusive" vs. fame-locked, and adding one is a server change out of scope) | `StatsStrip` |
| `src/components/entities/ControlsRow.tsx` | EM-2a: search input (raw `q`, debounced inside the hook) + status/fame `<select>`s — pure navigation for an operator tool, no system-behavior knobs (spine 3). Usability pass: the fame `<select>`'s options render as Established/Emerging/Unchecked (display copy only, `FameFilter` values unchanged) and carry a `title` explaining what each means | `ControlsRow` |
| `src/components/entities/EntityRow.tsx` | EM-2a: one table row's cells, each an inline correction control — a type `<select>`, a status Dismiss/Track toggle, Established/Emerging buttons (always paired with `fameLocked:true`, migrations/007's contract; display copy only — still PATCHes `fame:"famous"\|"not_famous"`) plus an explicit Unlock, "checked `<timeAgo>`"/"never checked" (honest time, spine 4), first-seen `<timeAgo>`. Every control takes `busy` and disables while its row's PATCH is in flight. EM-2b (operator feedback): the name is now a button that calls `onSelect(row.id)` to open the evidence panel (spine 2) instead of linking to Wikipedia — that link moved to a small icon beside the name (`title`/`aria-label` still name it as the fame verdict's evidence); a busy row dims (`opacity-50`) and shows an inline "saving…" label next to the name, not just disabled controls. Usability pass: every mutating control (type select, status toggle, both fame buttons, unlock) carries a `title` stating its consequence + "Reversible." before the click | `EntityRow` |
| `src/components/entities/EntitiesTable.tsx` | EM-2a: table shell (header + `EntityRow` list) plus a Prev/Next pagination footer (disabled at either edge) — 50 rows/page via `limit`/`offset`, no page-number picker. EM-2b: takes `onEntitySelect(id)`, threaded to each `EntityRow`'s name button; the pagination footer is now the shared `PaginationFooter` (also used by `CandidateTable`) instead of a locally-defined one. Usability pass: takes `pageSize`/`onPageSizeChange` and passes them + `PAGE_SIZE_OPTIONS` through to `PaginationFooter`'s opt-in rows-per-page control; the Fame column header carries a `title` with the one-line Established/Emerging helper text | `EntitiesTable` |
| `src/components/entities/PaginationFooter.tsx` | EM-2b: the Prev/Next footer extracted out of `EntitiesTable.tsx` so `CandidateTable.tsx` can reuse it byte-for-byte instead of duplicating it — a `noun` prop (default `"entities"`) is the only variation. Usability pass: gains an opt-in `pageSize`/`pageSizeOptions`/`onPageSizeChange` trio (all three required together) rendering a Rows-per-page `<select>` next to Prev/Next; `CandidateTable` doesn't pass them, so its fixed 100-per-page client pagination is byte-for-byte unchanged | `PaginationFooter` |
| `src/components/entities/ErrorToast.tsx` | EM-2b (Part A, operator feedback): fixed bottom-right, dismissible, never auto-hides — replaces the old inline top-of-tab error banner that went invisible once scrolled. Takes a plain `messages: string[]` so a caller can compose one or many lines (a single PATCH failure, or a bulk action's per-item failures) behind one dismiss. Usability pass: unchanged — still never auto-hides; `SuccessToast` (new, below) is the auto-dismissing half | `ErrorToast` |
| `src/components/entities/SuccessToast.tsx` | Usability pass, new: consequence narration's confirmation half — same fixed-corner shell convention as `ErrorToast` (rendered just above it, `bottom-20` vs `bottom-4`) but shows one `ToastState` message, auto-dismisses after 8s, and carries a persistent Undo button for that whole window when `toast.onUndo` is set. Clicking Undo fires the callback and dismisses immediately | `SuccessToast` |
| `src/components/entities/SegmentedControl.tsx` | EM-2b (Part C): the "Tracked" / "Review queue (N)" two-way toggle inside the Entities tab that replaces the standalone Review tab. Exports `EntitiesView` (`"tracked" \| "queue"`) | `SegmentedControl`, `EntitiesView` |
| `src/components/entities/CandidateRow.tsx` | EM-2b: one candidate-queue table row — select checkbox, display name + per-row type `<select>` (seeded from `typeHint`, overridable, shared by both the row's own Accept and any bulk accept it's part of), mention/source/day counts, `contexts` (when present), `coEntities` (when present), sample titles (each a `SampleTitleLink`), and the Accept/Merge (free-text target, local input state)/Dismiss actions — ports `ReviewTab.tsx`'s `CandidateCard` into a table row shape. Busy dims the row + "saving…", mirroring `EntityRow.tsx`. Usability pass: Accept/Merge/Dismiss each carry a consequence `title`; Merge's is the one control honestly marked "Not reversible here" rather than "Reversible" (no unmerge path exists) | `CandidateRow` |
| `src/components/entities/CandidateTable.tsx` | EM-2b: table shell for the candidate queue — header row includes a select-all-on-page checkbox, body is `CandidateRow`s, footer is the shared `PaginationFooter` (`noun="in review queue"`) | `CandidateTable` |
| `src/components/entities/BulkActionsBar.tsx` | EM-2b: appears only when `selectedCount > 0` — "N selected" plus "Accept selected" (each row uses its own current type)/"Dismiss selected", both disabled while `bulkBusy`. Usability pass: both buttons carry a consequence+reversibility `title` | `BulkActionsBar` |
| `src/components/entities/SampleTitleLink.tsx` | EM-2b (spine 2, required fix): resolves a candidate's sample title to its source article via `GET /api/articles?titleExact=` on click and opens it in a new tab; a 404 shows "article no longer retained" inline (an honest miss, not a dead link); a non-404 fetch failure shows a distinct "couldn't check, click to retry" state rather than conflating a real error with a confirmed-gone article | `SampleTitleLink` |
| `src/components/entities/index.ts` | EM-2a: barrel export for `entities/` sub-components + `types.ts`. EM-2b: adds the new candidate-queue components above plus `PaginationFooter`/`ErrorToast`/`SegmentedControl` (+ its `EntitiesView` type). Usability pass: adds `SuccessToast` | — |
| `src/components/SignalsTab.tsx` | Thin composition shell: imports hook + sub-components, composes layout — the signal **manager** (all states, client-side state filter) | `SignalsTab` |
| `src/components/signals/types.ts` | Shared client-side DTO shape for a signal row (mirrors `SignalJson`) and the lifecycle action union | `SignalCardData`, `SignalAction` |
| `src/components/signals/ManagedSignalCard.tsx` | Signal card with header, confidence bar, evidence expander (article links), entity chips, and a caller-supplied action button list — shared by BriefTab and SignalsTab | `ManagedSignalCard` |
| `src/components/signals/ManagedSignalGrid.tsx` | Grid of `ManagedSignalCard`s with per-state action sets (`reopen` only for dismissed) and an empty state | `ManagedSignalGrid` |
| `src/components/signals/StateFilterBar.tsx` | Client-side state filter buttons (All/New/Seen/Dismissed/Promoted) with live counts | `StateFilterBar` |
| `src/components/signals/utils.tsx` | Shared helpers: severityColor, timeAgo | `severityColor`, `timeAgo` |
| `src/components/signals/index.ts` | Barrel export for signals sub-components | — |
| `src/components/NetworkTab.tsx` | Force-directed graph of entity co-occurrences | `NetworkTab` |
| `src/components/MapTab.tsx` | Leaflet geospatial map with entity markers | `MapTab` |
| `src/components/FeedItemImage.tsx` | Image component with Google favicon and text-initial fallbacks | `FeedItemImage` |
| `src/hooks/useDashboardTable.ts` | Custom hook: all DashboardTable state, memos, effects, handlers. Default `activeTab` is `"brief"`. `TabKey` gained `"entities"` (EM-2a) — required so `setActiveTab`'s prop type stays a supertype of `HeaderBar.tsx`'s own (separately declared) `TabKey`; not on that task's literal allowlist but confirmed the sole `tsc` failure before touching it. `handleEntityClick` resolves a name to an entity id (opens `EntityPanel`) before falling back to the text filter; tracks `candidateCount` for the nav badge (EM-2b: now the Entities tab label's badge, fed by `EntitiesTab`'s candidate-queue segment instead of the deleted Review tab). EM-2b: `TabKey` drops `"review"` — `setPanelEntityId` is exposed unchanged and is now also passed to `TabContent` as `onEntitySelect`, reused by `EntitiesTab`'s row name-clicks (no new state needed, same global panel) | `useDashboardTable`, `TabKey`, `ColumnKey` |
| `src/hooks/useBusyIds.ts` | Per-id "in flight" tracking (a `Set`, not a single shared id) so one action's controls don't get wrongly re-enabled by an unrelated concurrent action | `useBusyIds` |
| `src/hooks/useBriefTab.ts` | Fetches/refreshes `/api/brief`, dispatches signal actions (POST-then-refresh) via `useBusyIds`, sequence-guards `load()` against out-of-order responses, distinguishes DB-not-configured from a fetch error | `useBriefTab`, `BriefData`, `BriefSignal`, `BriefNewEntity`, `BriefTopStory`, `BriefMover`, `BriefWarmup` |
| `src/hooks/useSignalsTab.ts` | Custom hook: signal-manager fetch/action/state-filter state (via `useBusyIds` + a sequence-guarded `load()`) — no longer carries the watchlist memos or theme builder, both removed with the Watchlist block | `useSignalsTab`, `StateFilter`, `STATE_FILTERS` |
| `src/hooks/useEntitiesTab.ts` | EM-2a: Entities-tab fetch/filter/PATCH state (mirrors `useSignalsTab.ts`'s shape) — debounces `q` 300ms; `load()` fetches `view=stats` and the filtered/paged list together via `Promise.all`, sequence-guarded like the other tab hooks; changing `q`/`status`/`fame`/`pageSize` resets `offset` to 0; `updateEntity` PATCHes `/api/entities/[id]` then ALWAYS refetches via `load()` in a `finally`, success or failure — a failed PATCH shows the error and still reflects the server's real state (fail loud, never silently revert) rather than leaving the last (possibly wrong) client-side value on screen. EM-2b (operator feedback, Part A): the PATCH failure message now lives in its own `actionError` state, separate from `error` (the list/stats fetch's own error) — previously both shared `error`, and `load()`'s unconditional `setError(null)` at the top of every call was wiping a just-set PATCH failure within one refetch cycle, making it effectively invisible. `dismissErrors()` clears both, for the tab's `ErrorToast`. Usability pass: `patchEntity` now returns and `updateEntity` applies the PATCH response (server truth) to `entities` state immediately via `applyServerUpdate` — fixes the live bug where a clicked row didn't visually update until the follow-up `load()` (or a manual refresh) landed; a row that no longer matches the active `status`/`fame` filters is dropped from the list and `total` decremented, everything else is a straight in-place replace, and `load()` still runs unchanged as reconciliation. `pageSize` is now state (`PageSizeOption`, default 50) instead of a module constant, with `setPageSize` resetting `offset`. A pure `describePatch(before, patch)` derives the `ToastState` (message + inverse `undoPatch`) for the five copy-specified actions (fame->Established/Emerging, Dismiss, Track, Unlock) from the pre-PATCH row snapshot; `updateEntity`'s optional `{silent:true}` (used by the toast's own `onUndo`) skips deriving/showing a toast for the undo call itself, so one-level undo never chains | `useEntitiesTab` |
| `src/hooks/useCandidateQueue.ts` | EM-2b: candidate-queue fetch/pagination/selection/bulk state for the Entities tab's "Review queue" segment (absorbs `ReviewTab.tsx`). `GET /api/candidates` is unchanged (returns the full qualifying set); pagination (100/page), per-row type overrides (seeded from `typeHint`, falling back to `"other"` for anything outside `ENTITY_TYPES`), and selection are all client-side. `load()` calls the caller-supplied `onCountChange(candidates.length)` on every fetch (success or failure, mirroring `ReviewTab.tsx`'s own `onCandidatesChanged` calls) — this is what keeps the Entities tab's nav badge live now that Review's own tab-mount refresh is gone. Single-row actions (`acceptOne`/`mergeOne`/`dismissOne`) POST the unchanged `/api/candidates` and always refetch in a `finally`, same fail-loud contract as `useEntitiesTab.updateEntity`; bulk actions (`bulkAccept`/`bulkDismiss`) POST the unchanged `/api/candidates/bulk` with each row's current resolved type, capped at 100 items by construction (page size = bulk cap), and surface per-item failures via `bulkErrors` without dropping the successes. Usability pass: `runRowAction` takes an optional `onSuccess(before)` so `acceptOne`/`dismissOne` can set a `ToastState` from the pre-action candidate snapshot without duplicating the fetch/busy/error plumbing (`mergeOne` doesn't — no toast copy was specified for merge). Accept's toast has no Undo (the spec's own undo path is "Dismiss in Tracked view", stated in the copy, not wired as a button). Dismiss's Undo has no candidate id to call back into — a dismissed candidate becomes a `dismissed` entity row (`candidate-actions.ts`'s `dismissCandidate`), so `retrackEntityByName` resolves it via the existing `GET /api/entities?name=` and PATCHes `{status:"tracked"}`, reusing endpoints rather than adding any (scope guard: no server changes) | `useCandidateQueue` |
| `src/hooks/useEnrichedEntities.ts` | Loads persisted analysis stores, enriches extracted entities with novelty scores via `novelty-scorer.ts`, then persists updated edge history/baselines via `signal-storage.ts` — unreferenced now that `useSignalsTab`'s watchlist (its only caller) is removed; left in place, out of this change's scope | `useEnrichedEntities` |
| `src/hooks/useSources.ts` | React hook for fetching feed data: tries DB-backed `/api/articles` first, falls back to live `/api/sources`. In db mode, an attention-driven passive freshness check — on load, on `visibilitychange` to visible, and on a ~5min interval while the tab is visible, no external pinger — pings unauthenticated `POST /api/tick` (no `?manual=1`) whenever `lastIngestAt` is over `STALE_INGEST_MS` (15min, mirrors `tick.ts`'s `FRESHNESS_THRESHOLD_MS`) stale; nothing fires while `document.hidden`, and the interval/listener are cleared on unmount and on mode change. Only `triggered:true` refetches (`applyPassiveTickResult`: immediate + one ~30s backup, same schedule the manual path uses); `reason:"fresh"`/`"locked"` are left for the next periodic/visibility check. Always silent — unlike `refresh()` below, this path never touches `refreshState`, which is reserved for the user's own click. `refresh()` itself: in live mode, unchanged (re-fetches `/api/sources`); in db mode, POSTs `/api/tick?manual=1` and drives an exported `refreshState` (`'idle' \| 'collecting' \| 'fresh'`) off the parsed `{triggered, reason}` — `triggered` or `reason:"locked"` both mean a run is in flight, so both show `'collecting'` and schedule `/api/articles` refetches at ~30s and ~90s (past `/api/articles`'s CDN edge-cache window); `reason:"fresh"` refetches once immediately and shows `'fresh'` for a few seconds; a failed or malformed tick response warns and falls back to one plain refetch, matching the old behavior | `useFeed` |
| `src/lib/types.ts` | All shared TypeScript interfaces | `FeedItem`, `SourceMeta`, `ExtractedEntity`, `EnrichedEntity`, `Signal`, `UrgencyLevel`, `SortConfig`, etc. |
| `src/lib/feed-fetcher.ts` | RSS/Atom fetching with 3-phase fallback (direct → relay → altUrl), parsing, and caller-owned cache | `fetchAllFeeds`, `parseFeedXml`, `parseRssItems`, `parseAtomEntries`, `CacheEntry` |
| `src/lib/entity-extractor.ts` | Dictionary-based NER from feed text with a hand-rolled lexicon sentiment scorer. `matchDictionaryEntries` (shared with `extract-v2.ts`'s dictionary layer): terms that are ACRONYMS in the dictionary (all-uppercase name/alias, 2-5 chars, e.g. "WHO", "UK", "ICE") match case-sensitively — an occurrence only counts if it's all-caps in the source — which also lifts the 3-char floor for them (so bare "UK" is matchable); every other term stays case-insensitive with the 3-char floor | `extractEntities`, `matchDictionaryEntries`, `isDictionaryTerm`, `scoreSentiment`, `LookupEntry` |
| `src/lib/entity-dictionaries.ts` | Country, org, region dictionaries and person stopwords. `ORG_DICT` includes an `ICE` entry (Immigration and Customs Enforcement) as a real acronym/word-collision example for the case-sensitivity fix — UK/EU/UN aliases on United Kingdom/European Union/United Nations were already present | `COUNTRY_DICT`, `ORG_DICT`, `REGION_DICT`, `PERSON_STOPWORDS` |
| `src/lib/novelty-scorer.ts` | Five-dimension novelty scoring (0–100) | `enrichEntities`, `computeCurrentEdges` |
| `src/lib/cascade-graph.ts` | Static causal graph across 12 impact domains with BFS chain builder | `buildCascadeChain`, `CascadeChain`, `CascadeDomain` |
| `src/lib/ad-filter.ts` | Ad and sponsored content detection with 50+ title patterns and URL/description filters | `isAdContent`, `isFinancialAd` |
| `src/lib/image-extractor.ts` | Six-method image URL extraction from RSS/Atom XML | `extractImageUrl`, `getSourceImageUrl`, `getDomainFromUrl` |
| `src/lib/xml-helpers.ts` | HTML stripping, XML tag/attribute extraction | `stripHtml`, `extractTag`, `extractAttr` |
| `src/lib/signal-storage.ts` | localStorage persistence for edge history, entity snapshots, baselines (muting removed — superseded by server-side dismiss; `loadPreviousEntityNames`/`saveEntitySnapshot`/`SNAPSHOT_INTERVAL` are currently unused now that the client detector they fed is gone, kept per explicit task scope) | `SNAPSHOT_INTERVAL`, `loadPreviousEntityNames`, `saveEntitySnapshot`, `edgeKey`, `loadEdgeHistory`, `saveEdgeHistory`, `loadEntityBaselines`, `updateEntityBaselines`, `getBaselineRate`, `EntityBaseline` |
| `src/lib/geo-coordinates.ts` | Lookup map of 100+ countries/regions to [lat, lng] centroids | `getCoordinates` |
| `src/lib/date-utils.ts` | Relative and absolute date formatting | `timeAgo`, `formatDate` |
| `src/lib/urgency.ts` | Deterministic category-to-urgency mapping and Tailwind styling | `getUrgencyLevel`, `getRowClasses`, `getUrgencyBadgeClasses` |
| `src/lib/theme.ts` | Dark/light theme class generation | `getThemeClasses`, `ThemeClasses` |
| `src/lib/sources-data.json` | Static list of ~147 RSS/Atom feed sources with name, category, tier, url, type | — |
| `src/lib/__tests__/ad-filter.test.ts` | Tests for ad/sponsored content detection | — |
| `src/lib/__tests__/xml-helpers.test.ts` | Tests for HTML stripping and XML extraction | — |
| `src/lib/__tests__/image-extractor.test.ts` | Tests for image URL extraction from XML | — |
| `src/lib/__tests__/urgency.test.ts` | Tests for category-to-urgency mapping and styling | — |
| `src/lib/__tests__/date-utils.test.ts` | Tests for timeAgo and formatDate | — |
| `src/lib/__tests__/feed-parser.test.ts` | Tests for RSS 2.0 and Atom parsing | — |
| `src/lib/__tests__/entity-extractor.test.ts` | Tests for entity extraction, co-occurrence, sentiment; a case-sensitivity table for acronym-form dictionary terms (WHO/who, UK/uk, ICE/ice) | — |
| `src/lib/__tests__/entity-dictionaries.test.ts` | Tests for the 49 US states + DC added to `REGION_DICT`: all present as full names only (no 2-char forms), Georgia (state) deliberately absent and Washington (state) renamed to "Washington State" (both would otherwise silently override an existing country/alias resolution), zero *new* cross-dictionary name/alias collisions, end-to-end resolution via `matchDictionaryEntries` | — |
| `src/lib/server/__tests__/article-identity.test.ts` | Tests for contentHash stability, normalization, distinctness | — |
| `src/lib/server/__tests__/ingest-writer.test.ts` | Tests for persistArticles (dup-group linking, batching) and sweepRetention (mocked sql client) | — |
| `src/lib/server/__tests__/extract-v2.test.ts` | Tests for normalizeName and each extractCandidates layer (incl. product-pattern's digit-token/interior-caps shapes and stoplist), dedup tie-break (incl. llm/product-pattern layer priority, roleContext and prominence carry-through), U.K./Great Britain/bare-UK dictionary resolution, AI acronym-stoplist suppression, Kane-on-ice acronym collision | — |
| `src/lib/server/__tests__/entity-ingest.test.ts` | Tests for resolution order, rollup math (hourly/edges/relations/candidates/contexts/co_entities), mention dedup, idempotency query shape, chunkArticles (mocked `llm-extract`: `submitBatch`/`pollBatch`/`fetchBatchResults`). M-batch: a describe block per pending-batch branch — no pending row submits a new batch and processes nothing this tick vs. a submit failure/budget-gate block heuristic-processes immediately, tallying the reason; still-`in_progress` (and a poll failure) leaves the row's articles unselected, skips submitting, and skips the heads query entirely; abandoned (6h+) heuristic-processes without polling; `isLlmConfigured()` false with a row present heuristic-processes immediately; `ended` applies a succeeded chunk through the real parser (contexts/co_entities carry through), an errored/canceled/expired chunk and a results-fetch failure both fall back to heuristics, and an exactly-once-guard case where the pending row's second article is absent from the re-query is silently skipped, not double-written. Famous-vs-known/obscure auto-accept branching and relation persistence (both-endpoints-resolve incl. same-run auto-accept, unresolved-endpoint drop, self-loop drop) still covered via the `ended`-branch fixtures (mocked sql client); hourly bucketing keys on effectiveAt-only, edges/relations key on arrivalAt-only | — |
| `src/lib/server/__tests__/llm-extract.test.ts` | Tests for isLlmConfigured, estimateCostUsd (live + batch pricing, summed)/getLlmMonthStats (live+batch columns), `submitBatch` request shape (one request per chunk at the batches endpoint/headers/model/summary truncation/system-prompt ontology&relation-vocab coverage, chunk-too-large refusal, API-key-never-logged), budget gating (live spend, already-recorded batch spend, at/over cap, a usage-read failure), submit failure reasons (non-2xx, network-throw, malformed response), `pollBatch` (in_progress, canceling treated as in-flight, ended+resultsUrl, ended-with-no-results_url, non-2xx, network-throw), `fetchBatchResults` (entity type mapping incl. legacy place→region, unknown-type-kept-as-other, prominence defaulting, relation parsing incl. unknown-type-dropped/endpoint-not-listed, markdown-fence stripping, per-chunk errored/canceled/expired/malformed via `chunkFailures`, out-of-order custom_id resolution, malformed-line skipping, cross-chunk usage summation, results-fetch non-2xx/network-throw), `recordBatchUsage` upsert shape (batch columns only, live columns untouched) — all against a stubbed `fetch` with a per-test guard so no path can reach the real network | — |
| `src/lib/server/__tests__/settings.test.ts` | Tests for DEFAULTS overlay, type-validation fallback per key (mocked sql client) | — |
| `src/lib/server/__tests__/run-ingest.test.ts` | Every stage dependency mocked wholesale — exists specifically to cover the fame-sweep stage's wiring, added after detect-signals. Tests: `fameSweep` stats included in the response on success, running after the signals stage (call-order assertion); M008: the response's `fameSweep` includes `failureReasons` byte-for-byte (proving the tally reaches the ingest JSON response, not just console logs) and a separate case asserts an `abortedReason` marker also passes through untouched; a fame-sweep failure logs loudly via `console.error` and does not abort the run (status 200, `fameSweep: null`, other stages' counts still present, compute-warmup still runs afterward); an unrelated stage (persist-articles) still 500s the whole run, unchanged, confirming the fame-sweep stage's non-abort behavior is a deliberate deviation, not a regression in `runStage` itself (mocked `fetchAllFeeds`/`persistArticles`/`sweepRetention`/`processNewArticles`/`getSettings`/`runDetectors`/`computeWarmupState`/`persistSignals`/`runFameSweep`) | — |
| `src/lib/server/__tests__/wikidata.test.ts` | Tests for `computeWindowMonths` (hand-checked 12-month window ending 2 months before `now`, incl. a January year-rollover case); `computeMedianPageviews` — the spike-immunity linchpin: a 3-month-old article with huge recent views over the full 12-month window medians to 0, plus odd/even-length window boundary cases (both zero-filled and genuine-sustained-history); every pure parser (`parseSearchResponse`/`parseSearchResponseIds`, `parseEntitiesResponse`/`parseEntitiesResponseMulti` incl. multi-language dedup/no-enwiki/malformed-entity/missing-id-doesn't-corrupt-others, `parseBatchEntitiesResponse` incl. exact-title match, case/underscore-insensitive match, redirect-title mismatch left unresolved, missing-title placeholders skipped, `parsePageviewsResponse` incl. malformed-entry skipping); `ALIAS_LANGUAGES`'s exact value; M008: `USER_AGENT` asserted to carry a contact address. `lookupWikidataFame` end to end — full success sequence with User-Agent asserted on every request, `limit=5` asserted on the search call, no-match short-circuits after one call, no-enwiki-sitelink skips the pageviews call, a pageviews 404 resolves to a zero median (not a failure); M008: a parameterized `it.each` matrix asserts the specific reason string (403/429/500/malformed-body/timeout) at each of the 3 call stages (search/entities/pageviews) — 20 cases; a disambiguation block models the real ICC false-match case (a 2-sitelink first result loses to a 100-sitelink later one), ties favor the earlier result, no-candidate-has-enwiki keeps the original top-hit-only behavior, single-candidate responses are unaffected; `lookupFameFromMatch` tested directly (null wikiTitle skips the pageviews call, a present one fetches and medians correctly, a pageviews failure surfaces its typed reason); `resolveNamesBatch` tested directly (one request for 50 names with the right URL shape, exact-title resolution, a response missing some titles doesn't corrupt the ones present, an empty name list never calls fetch, 429/timeout produce typed failures) — all against a stubbed `fetch` with the same per-test throw-if-unmocked guard as `llm-extract.test.ts` | — |
| `src/lib/server/__tests__/fame.test.ts` | Tests for the dictionary prong (via canonical name and via alias, case-insensitive), breadth prong boundary (11 vs 12), the p75 volume-threshold percentile math (hand-checked exact-index and interpolated cases, the floor of 3, `Infinity` for an empty population), the zero/negative-baseline volume guard, the composed `isFamous` (each prong alone, none, and the candidate-path reduction with zero baseline/breadth), and `loadLifetimeSourceBreadth`'s empty-id short-circuit, row mapping, and no-`dup_group_id`/no-time-window query shape (mocked sql client). M006: `computeWikiFameVerdict` boundaries (19,999 vs 20,000 pageviews; 24 vs 25 sitelinks with and without 3,000-pageview corroboration; 100 sitelinks at zero pageviews stays not_famous; no-match); stored-verdict `isFamous` integration (`storedFame='famous'` overrides every heuristic prong being negative; `storedFame='not_famous'` does NOT veto a firing heuristic prong; `'unknown'` is a no-op) | — |
| `src/lib/server/__tests__/fame-sweep.test.ts` | Tests for `selectSweepBatch`'s query shape (tracked status, the `unknown`/6-hour (`make_interval`) and `not_famous`/30-day recheck windows, `'famous'` absent from the WHERE clause, the never-checked-first/newest-first_seen_at ORDER BY, the LIMIT value of 40, M007: `fame_locked = false` present in every branch); a real-Postgres boundary suite (`TEST_DATABASE_URL`-gated, reusing `helpers/pg-sql.ts`) proving the actual selection outcome at each edge — unknown checked 5h ago not reselected vs 7h ago reselected, not_famous checked 29d ago not reselected vs 31d ago reselected, famous never reselected regardless of age, a never-checked row still reselected, M007: a locked never-checked-unknown row and a locked 31-day-old not_famous row are both never reselected though their unlocked equivalents would be; a successful lookup writes all five columns (`fame`, `wiki_title`, `wiki_sitelinks`, `wiki_pageviews_monthly`, `fame_checked_at`) — including a not-famous verdict, still counted as a success, not a failure; a RETURNED failed lookup writes ONLY `fame_checked_at` (query asserted to omit `fame =`/`wiki_title`); dictionary aliases (Ukraine → Kyiv/Kiev) still merge in on a network failure; the wall-clock budget stops early (checked between chunks, not entities) but processes normally when comfortably inside it; a 40-entity batch runs in chunks of 3 with each chunk's entities concurrent (an in-flight/max-in-flight counter on a stubbed `fetch` proves concurrency never exceeds 3); a deadline expiring mid-chunk still lets that in-flight chunk finish before the remaining chunks are skipped; mixed success/failure within one concurrent chunk aggregates correctly (every mock's fetch sequence updated for M008's upfront batch-resolution call — a "no match" response prepended so the per-name sequence beneath it is unaffected). Poison-guard block (`../wikidata` mocked so a canonical name containing `"POISON"` makes `lookupWikidataFame` reject instead of its normal never-throws contract, every other name — including `resolveNamesBatch`/`lookupFameFromMatch`, left real — delegating to the real implementation): a THROWN lookup degrades to a failure outcome tagged `exception_Error` with the sanitized sample message, stamps only `fame_checked_at`, logs exactly one `[fame-sweep] entity check threw` line naming the id and `JSON.stringify`'d canonical name, leaves its chunk-mates' successes unaffected, and the sweep still reaches a later chunk (stats count every entity across both); a successful lookup whose full write throws degrades the same way; the rescue write itself throwing propagates out of `runFameSweep` uncaught. M008: a `failureReasons` tally block asserts distinct reasons aggregate correctly across a mixed batch without tripping the circuit breaker; a circuit-breaker block asserts a 429 (and separately a 403) partway through a chunk aborts every later chunk while the current chunk's outcomes are kept, and — the subtle part — chunk-2+ entities are proven completely unwritten (no `UPDATE entities` call for their ids at all, so `fame_checked_at` is untouched and they retry on the normal schedule), plus a case where the rate limit hits the upfront batch call itself, aborting before any entity is attempted; a batched-resolution block asserts an exact-title match skips straight to pageviews (only 2 fetch calls total) and an unresolved name (e.g. "Putin") falls through to the full 4-call per-name path. M009: an `exceptionSamples` block asserts five identical crashes collapse to one sample line, five DISTINCT crash messages are capped at three samples while all five still count in `failureReasons`, and the field is absent entirely when nothing threw; a `describeException` unit block covers the reason key (`Error`/`TypeError`), the `Name: message` sample shape, redaction of a whole-message `postgresql://user:pass@host/db` connection string and of a URL embedded mid-sentence, the 160-char message cap, thrown non-`Error` values (a string, `null`), and error-name sanitization (punctuation stripped; an all-punctuation name falls back to `Unknown`). `mergeAliases` unit tests (dedup via `normalizeName`, existing aliases never dropped even past the 24 cap, new aliases capped at 24 total, no self-referential canonical-name alias, dictionary aliases only when `canonical_name` matches a `DictEntry.name`, case/diacritic-insensitive dedup against an existing alias) — all against a stubbed `fetch` with the same per-test throw-if-unmocked guard as `llm-extract.test.ts` | — |
| `src/lib/server/__tests__/entity-admin.test.ts` | Tests for `findSimilarNames` (the "Air Force"/"Air Force One" whole-word-prefix case, the "Modi"/"Narendra Modi" whole-word-suffix case, the "U.S. military"/"US military" punctuation-stripped exact-match case, unrelated names NOT grouping, no false-positive substring-within-a-word match, no false match between two different same-length names, order-independence, and transitive three-way grouping via connected components); `searchEntities` (canonical/alias match, status/fame pushed down to SQL, invalid status/fame rejected, default limit 50, limit capped at 200, a malformed limit or offset REJECTED rather than coerced (`NaN`, non-integer, zero, negative), offset paging, the zero-results shape, and the evidence contract — every row carries `wikiTitle`/`wikiSitelinks`/`wikiPageviewsMonthly`/`fameCheckedAt`, with a never-checked entity's `fameCheckedAt` distinguishable from a checked one's); `loadEntityStats` (parked vs never-checked counted distinctly, the empty-database all-zero/null shape, single-query assertion) — mocked sql client throughout | — |
| `src/lib/server/__tests__/detectors.test.ts` | Tests for each scoring function on synthetic panels (flat/no-surge, 10x-spike/critical, cold-start skip, severity boundaries, recalibrated first-seen/novel-edge tables), bootstrap-cohort suppression, `computeWarmupState` boundary, `computeEffectiveBaselineDays` λ math, tracked/cluster-head query shape (mocked sql client). `runDetectors — novel-edge fame suppression`: a famous-famous edge (real COUNTRY_DICT names) is suppressed, a famous-satellite and a satellite-satellite edge both still fire, and surge/sentiment/cross_category/first_seen are asserted byte-identical whether or not a famous-famous edge is present in an otherwise-unchanged panel. M006: a stored-famous, heuristically-invisible endpoint pair is suppressed (both-famous via `storedFame` alone), while a stored-famous+plain-satellite pair still fires | — |
| `src/lib/server/__tests__/signal-store.test.ts` | Tests for cooldown suppression, created/refreshed counting, transition legality matrix, severity ordering, evidence-article resolution (mocked sql client) | — |
| `src/lib/server/__tests__/brief.test.ts` | Hand-checked tests for computeStoryScore's ranking formula and computeLift (incl. the 0.5 baseline floor); getBrief tests for warm-up-gated empty movers, lift ranking/threshold/cap (mocked sql client). M006: `loadNewEntities`'s query excludes `fame='famous'` (query-shape assertion); a non-famous row still passes through into `brief.newEntities` unchanged | — |
| `src/lib/server/__tests__/developments.test.ts` | 20 `getDevelopments` fixtures (mocked sql client, query-substring-routed) plus unit tests for every pure scoring/eligibility/assembly function. Fixtures 1-14 cover the original L1A eligibility/scoring/evidence-integrity rules (anchor-by-type/volume, distinct-source/anchor floors, staleness penalty, candidate title-match ambiguity, bootstrap exclusion). L2A additions: fixture 15-16 the breaking-satellite candidate shortcut (day_count 1 + 3 vs 2 sources); 17 a famous-famous relation (anchor + dictionary-famous non-anchor) suppressed though the old isAnchor-only rule would have produced a card; 18 a dictionary-famous low-volume org serving as context for a genuine satellite — the deliberate R widening, previously no card at all; 19 a satellite-satellite relation; 20 a famous-by-breadth E subject rejected by the uniform eligibility gate despite clearing the (unwidened) isAnchor split; a dedicated instant-ubiquity describe block (6 vs 5 distinct N-entity sources within 48h). M006: a stored-famous, heuristically-invisible R-source subject is excluded; a stored not_famous verdict on a dictionary-famous entity (NATO) does not veto its own firing dictionary prong, still suppressing the famous-famous relation | — |
| `src/lib/server/__tests__/tick.test.ts` | Tests for `isRecent`'s boundary math at both the 15min freshness and 10min lock thresholds (incl. the null/no-data-yet case); `selectFreshnessThreshold`'s manual/passive selection (incl. `MANUAL_FRESHNESS_THRESHOLD_MS === LOCK_THRESHOLD_MS` and a 12-minute-old timestamp that's stale under the manual threshold but fresh under the passive one); `tryAcquireLock`'s query shape (`ON CONFLICT ... DO UPDATE ... WHERE ... RETURNING`, `make_interval(mins => 10)`) and its true/false mapping from rows-returned (mocked sql client) | — |
| `src/lib/server/__tests__/helpers/pg-sql.ts` | Test-only `Sql` adapter over a real `pg` `Pool` (no Neon proxy dependency) plus a from-scratch migration applier, for integration tests | `makePgSql`, `applyMigrations`, `freshSchema` |
| `src/lib/server/__tests__/integration/env-canary.test.ts` | Ungated: fails loudly if `CI` is set but `TEST_DATABASE_URL` is not, so the integration layer can't silently skip in CI | — |
| `src/lib/server/__tests__/integration/ingest-writer.integration.test.ts` | Real-Postgres tests for `persistArticles` (exact-duplicate/dateless/cross-source dup-group linking, recurring-headline head rotation across a 48h window) and `sweepRetention` (30-day delete, `ON DELETE SET NULL` on a dup-group head) | — |
| `src/lib/server/__tests__/integration/entity-ingest.integration.test.ts` | Real-Postgres tests for `processNewArticles` (dictionary entity creation, cluster-head-only mentions, hourly/edge/candidate rollups, idempotent re-run incl. the v2 stats shape, `last_seen_at` advancement on ARRIVAL even with a backdated `published_at`, SQL-level candidate accept flow) | — |
| `src/lib/server/__tests__/integration/llm-extract.integration.test.ts` | M-batch: real-Postgres, mocked-`fetch` tests against the two-tick submit-then-apply flow (tick 1 submits and processes nothing; tick 2 polls/applies). `llm_usage`'s batch columns accumulate across two resolved batches; `contexts`/`co_entities` carry through the full pipeline into `entity_candidates` once applied; a submit failure (non-2xx) processes the article heuristically the SAME tick, tallying the reason; an errored chunk falls back to heuristics once results are applied; an abandoned (6h+, `submitted_at` fast-forwarded) pending batch heuristic-processes without ever calling poll; asserts the mocked `fetch` was called against the batches endpoint. Hyundai/Boston Dynamics scenario (now via `submitAndApply`'s two-tick helper): a famous company auto-tracks while an obscure company and a person (role "CEO") land in `entity_candidates`, the relation is dropped while an endpoint is unresolved, then written with correct direction once that endpoint is accepted and the story recurs; re-run idempotency on `entity_relations.article_count`; migration 008's `llm_batches` table and `llm_usage`'s batch columns usable on a fresh schema | — |
| `src/lib/server/__tests__/integration/signal-engine.integration.test.ts` | Real-Postgres tests for the full signal pipeline: `runDetectors`+`persistSignals` producing a correct surge row, re-run refreshing (not duplicating) under the partial unique index, dismiss-cooldown suppression, the transition state machine, `loadSignals`'s entity-name and evidence-article resolution, `getBrief`'s ranked top stories. Seeds an old (30-day) system-epoch article in `beforeEach` so these tests, which mostly never touch `articles`, aren't wrongly warm-up-gated | — |
| `src/lib/server/__tests__/integration/warmup-gate.integration.test.ts` | Real-Postgres tests for the warm-up gate: a day-1-launch scenario (articles arriving within 24h, `published_at` spread across the prior week) yields zero surge/first_seen/novel_edge while cross_category still fires; an old (9-day) system epoch lets first_seen fire at warning (not critical) for 3 sources and a 10x spike fires surge with a sane z (< 15σ) | — |
| `src/lib/server/__tests__/integration/tick.integration.test.ts` | Real-Postgres tests for `tryAcquireLock`: first caller wins an absent lock, a second immediate caller loses, two concurrent callers racing an absent lock produce exactly one winner, a lock older than the threshold is reclaimed, one still within the threshold is not | — |
| `src/app/api/articles/__tests__/route.test.ts` | Tests for 503-when-unconfigured/empty, `updatedAt`/`clusterSize` presence and sourcing from the `cluster_updates` CTE, `ORDER BY cu.updated_at DESC`, category-filter branch routing and value binding (mocked sql client) | — |
| `src/app/api/articles/__tests__/route.integration.test.ts` | Real-Postgres tests: a dup-group member's later arrival becomes the head's `updatedAt` (not the head's own `first_seen_at`); a head with no members has `updatedAt` equal to its own `first_seen_at` and `clusterSize` 1 (no chip); a head with two members has `clusterSize` 3 (chip math: +2); results sort by the cluster's `updatedAt`, not publish time | — |
| `src/app/api/candidates/__tests__/route.test.ts` | Tests for GET filtering/shaping (incl. contexts/coEntities passthrough and empty-array defaulting) and POST accept/merge/dismiss incl. validation and conflict 409s; accepts a new-ontology type (e.g. `government_body`) the old 5-type whitelist would have rejected. M006: the POST suite now stubs `fetch` (a safe default "no Wikidata match" response, since every accept transitively reaches `fame-sweep.ts`'s inline check) — a dedicated "inline fame check after accept" block asserts the fame write happens with the response contract unchanged, that a failed lookup still 200s writing only `fame_checked_at`, and that a conflict-resolved accept preserves the pre-existing entity's real aliases instead of wiping them (M007: unchanged, still green after the shared logic was exported) | — |
| `src/app/api/candidates/bulk/__tests__/route.test.ts` | Tests for 503/400s (invalid JSON, non-object body, invalid action, non-array items, empty items, >100 items rejected, exactly 100 accepted), a per-item "Candidate not found", a per-item error for accept missing/invalid `type`, a successful accept (asserting the inline fame-check write still happens) and dismiss, a per-item naming-conflict error, and a mixed-outcome batch (one success, one not-found) proving partial failure surfaces in one 200 response rather than aborting | — |
| `src/app/api/entities/__tests__/route.test.ts` | Tests for name/alias resolution and deterministic ordering (unchanged). M007: a new "list mode" describe block covers `?name=` entirely absent returning `{entities, total}`, the zero-results shape, `q` filtering, and 400 on a malformed limit or an invalid status. EM-2a: a new `?view=stats` describe block covers 503-when-unconfigured, the shape round-tripping through `loadEntityStats` (mocked one-row aggregate), an all-zero/null-timestamp empty table, and `view=stats` taking priority even when other (otherwise-invalid) list params are also present | — |
| `src/app/api/entities/[id]/__tests__/route.test.ts` | Tests for entity profile + series/articles/edges query shape + incoming/outgoing relations (one query each, correct direction) — unchanged. M007: a new `PATCH` describe block covers 503/400/404, each field validated and applied independently, `status` round-tripping both ways, setting `fame` implying `fame_locked=true`, `{fameLocked: false}` releasing a lock, an explicit `fameLocked` winning even when `fame` is set in the same request, and the returned row carrying its full evidence shape | — |
| `src/app/api/signals/__tests__/route.test.ts` | Tests for state-csv parsing/defaulting, action validation, 404/409 semantics, legal transition wiring | — |
| `src/app/api/brief/__tests__/route.test.ts` | Tests for 503-when-unconfigured and the response shape + cache header | — |
| `src/app/api/developments/__tests__/route.test.ts` | Tests for 503-unconfigured, warm-up skipping the engine, response shape + cache header, and limit validation (default/custom/boundary/reject-invalid) | — |
| `src/app/api/tick/__tests__/route.test.ts` | Tests for 503-when-unconfigured, the fresh/locked short-circuits (incl. a null `lastIngestAt` — no articles yet — being treated as stale, not fresh), the winner running `runIngest` and returning `{triggered:true, ...stats}`, an ingest failure's status propagating with `triggered:true` still set, and GET behaving identically to POST (mocked sql/tick/run-ingest modules). Separate `?manual=1` suite: a 15-minute-old ingest is stale under the manual 10min threshold (attempts the lock); a 12-minute-old ingest is fresh under the passive default (no query) 15min threshold; a 2-minute-old ingest is fresh even under manual; every non-`"1"` value (`"true"`, `"0"`, `"2"`, `"yes"`, `""`) and unrelated extra params are ignored and fall back to passive (tested with a 5-minute-old ingest); GET honors `?manual=1` the same as POST | — |
| `src/hooks/__tests__/useSources.test.ts` | `@vitest-environment jsdom` (this repo's first hook-rendering test, via `@testing-library/react`'s `renderHook`). Passive-check suite: fires a plain `POST /api/tick` (no `?manual=1`) when `lastIngestAt` is stale (>15min) and the document is visible; does not fire when fresh (<15min); does not fire while `document.hidden` (via an overridden `visibilityState`/`hidden`), even when stale; a `visibilitychange` to visible triggers an immediate check; `refreshState` stays `'idle'` throughout even when the response is `triggered:true`; the interval and `visibilitychange` listener are cleared on unmount (spied `clearInterval`/`removeEventListener`). Separate `refresh()` suite (all against a 5min-old `lastIngestAt`, fresh enough that the passive check above never interferes): `triggered:true` and `reason:"locked"` both set `refreshState` to `'collecting'` and schedule `setTimeout`s at 30_000ms and 90_000ms; `reason:"fresh"` refetches `/api/articles` once immediately and sets `refreshState` to `'fresh'`; a rejected or non-ok tick request warns and falls back to exactly one plain refetch with `refreshState` left `'idle'`; live mode's `refresh()` re-fetches `/api/sources` directly and never calls `/api/tick` | — |
| `src/hooks/__tests__/useSignalsTab.test.ts` | `@vitest-environment jsdom`. Tests that `warmup` (from `/api/signals`) parses into a well-shaped `{active, daysRemaining}` object, defaults to `null` when the response omits it, and defaults to `null` when the field is malformed (mocked `fetch`) | — |
| `src/hooks/__tests__/useDashboardTable.test.ts` | `@vitest-environment jsdom`. Tests that the default Time-column sort orders by `updatedAt` (not publish time) when they disagree, and falls back to publish time when `updatedAt` is absent (live mode) — both via `renderHook` against a mocked `fetch` | — |

## Invariants

- **7-day item window**: Feed items older than 7 days are discarded at fetch time. No stale data enters the pipeline.
- **Dateless item published_at**: feed items with no parseable date get `publishedEstimated: true` on the `FeedItem` plus a `now()`-stamped `published` for client display; `persistArticles` stores `published_at = NULL` for these rows in Postgres instead of the stamp.
- **15 items per source**: Each feed is capped to prevent any single source from dominating.
- **Deterministic urgency**: Urgency level is a pure function of source category — no mutable state, no runtime overrides.
- **Feeds rows carry no severity**: `FeedTable`/`FeedCardList` no longer call `getUrgencyLevel`/`getRowClasses`/`getUrgencyBadgeClasses` — every row uses the same neutral alternating background and the same quiet, uniform category chip (both themes), regardless of category. `urgency.ts` is unchanged and still consumed by `entity-extractor.ts` (and, through it, `MapTab`/`NetworkTab`) — category-derived severity coloring simply no longer reaches the Feeds tab, since it was never an earned signal. Alarm styling is now exclusively the Signals tab's, earned by actual deviation from an entity's own baseline.
- **Cluster weight, not alarm**: `/api/articles`'s `cluster_updates` CTE also emits `cluster_size` (`COUNT(*)` per cluster, alongside `updated_at`); `FeedItem.clusterSize` carries it through (db mode only — same contract style as `updatedAt`, absent in live mode). `FeedTable`/`FeedCardList` show a muted "+K" chip (K = `clusterSize - 1`, `title="K more articles about this story across sources"`) next to the source name whenever `clusterSize >= 2` — quieter styling than the category chip, since it communicates coverage weight, not severity.
- **Canonical entity names**: Dictionary-based extraction resolves aliases to canonical forms (e.g., "USA" → "United States"). Case-insensitive, word-boundary matching, longest-match-first.
- **Bidirectional co-occurrences**: Entity pairs are stored as sorted tuples — no duplicates, order-independent.
- **Novelty score range**: Composite score is 0–100 across five dimensions; no single dimension can exceed its cap (spread: 30, diversity: 15, edges: 25, surprise: 20, emergence: 10).
- **Single Neon Postgres, written by `/api/ingest`, `/api/tick`, `/api/candidates` POST, `/api/candidates/bulk` POST, `/api/entities/[id]` PATCH, and `/api/signals` POST**: server-side persistence (articles, entities, mentions, edges, candidates, signals) lives in one Neon Postgres database. `/api/ingest` (GitHub Actions cron, two schedules per hour + daily Vercel cron fallback) and `/api/tick` (unauthenticated self-heal — see the invariant below) both call `run-ingest.ts`'s `runIngest`, the only entry point into the article/entity/signal pipeline; `/api/candidates` POST, `/api/candidates/bulk` POST, `/api/entities/[id]` PATCH, and `/api/signals` POST are the other writers (reviewer/user-triggered, all unauthenticated by design — single-user hobby app, no login exists anywhere in the system). `/api/articles`, `/api/entities` (both modes), `/api/entities/[id]` GET, `/api/candidates` GET, `/api/signals` GET, and `/api/brief` are read-only. Client-side ephemera (edge history, entity snapshots, baselines, theme) still lives in browser localStorage.
- **Freshness follows attention, no external pinger**: `/api/tick` (`src/app/api/tick/route.ts`) is deliberately unauthenticated and reads nothing from the request (no body, no query params — POST/GET take no `req` argument at all), because it can only ever do what the hourly cron already does. It returns `{triggered:false, reason:"fresh"}` immediately when `MAX(articles.first_seen_at)` is under `tick.ts`'s `FRESHNESS_THRESHOLD_MS` (15min) old. Otherwise it attempts the anti-stampede lock: `tryAcquireLock` issues one atomic `INSERT INTO settings ... ON CONFLICT (key) DO UPDATE ... WHERE <stored tick_lock timestamp> <= now() - 10min RETURNING key` — the `WHERE` clause on the `DO UPDATE` is the sole arbiter under concurrent callers (Postgres re-evaluates it per-row once lock contention resolves), so exactly one concurrent caller ever gets a row back. Only that winner calls `runIngest` (the identical pipeline `/api/ingest` runs) and returns its stats plus `{triggered:true}`; every loser returns `{triggered:false, reason:"locked"}` without touching the pipeline. `isRecent(timestamp, now, thresholdMs)` is the one pure function behind both thresholds (15min freshness, 10min lock), injectable-`now` for unit testing. `useSources.ts`'s `useFeed` is the client-side trigger, checked on load, on `visibilitychange` to visible, and on a ~5min interval while the tab is visible (nothing fires while `document.hidden`): whenever `lastIngestAt` is over 15min stale, it fires `POST /api/tick` (passive, no `?manual=1`); only a `triggered:true` response refetches `/api/articles` (immediate + one ~30s backup), while `reason:"fresh"`/`"locked"` are left for the next check. This path is always silent — it never touches `refreshState`, which is reserved for the user's own Refresh click (see the next invariant). The interval and `visibilitychange` listener are cleared on unmount and on mode change. Combined with `.github/workflows/ingest.yml`'s twice-hourly schedule and its job failure on `feedsSucceeded === 0`, a dead GitHub Actions scheduler no longer silently starves ingestion for hours — any dashboard view within 15min of staleness triggers a real ingest run on its own, as long as someone is actually looking.
- **The Refresh button honestly triggers collection, not just a DB read**: `?manual=1` on `/api/tick` (exact-match query flag, everything else about the route's no-body/no-other-params contract unchanged) swaps `tick.ts`'s `selectFreshnessThreshold` from the 15min passive window down to `MANUAL_FRESHNESS_THRESHOLD_MS` — an alias for `LOCK_THRESHOLD_MS` (10min), since the lock is the actual abuse ceiling regardless of which threshold let a request past the freshness gate, and a redundant `runIngest` is cheap (`persistArticles` dedupes by content hash, so a run that finds nothing new does no entity/LLM work). `useSources.ts`'s `refresh()`, in db mode, POSTs `/api/tick?manual=1` and maps the parsed `{triggered, reason}` onto an exported `refreshState`: `triggered:true` or `reason:"locked"` (someone else's run is already in flight — indistinguishable to the user, handled identically) both set `'collecting'` and schedule two `/api/articles` refetches, ~30s and ~90s out, then settle to `'idle'` — two refetches because `/api/articles` sits behind a CDN edge cache (`s-maxage=60`), so the 30s one can still race it; the 90s one is the one guaranteed to land after it rolls over. `reason:"fresh"` refetches once immediately and holds `'fresh'` for a few seconds before settling to `'idle'`. A failed fetch, a non-ok response, or a malformed body all fall back to the pre-existing behavior: `console.warn` plus exactly one plain refetch, `refreshState` untouched. Pending scheduled refetches are tracked in a ref and cleared both at the start of each new `refresh()` call and on unmount. `HeaderBar.tsx`'s Refresh button surfaces `refreshState` directly (disabled + "Collecting… ~1 min" / "Up to date" / "Refresh"); live mode's `refresh()` is unchanged — it already re-fetches feeds directly.
- **Signal lifecycle**: a signal row's `state` moves `new` → (`seen` | `dismissed` | `promoted`) any number of times among the three active states, and `dismissed` → `new` only via an explicit `reopen` — no other transition out of `dismissed` is legal. `signals_dedupe_key_active_idx` is a partial unique index on `dedupe_key` covering only the active states (`new`, `seen`, `promoted`); a dismissed row is deliberately excluded from that constraint so a fresh detection can create a brand-new active row for the same `dedupe_key` once `persistSignals`'s dismiss-cooldown window (`settings.dismiss_cooldown_hours`, default 72h) has elapsed. Re-detection never touches `state` — only `last_evidence_at`/`confidence`/`severity`/`evidence`/`title` are refreshed on an existing active row. `transitionSignal` is a single conditional `UPDATE ... WHERE id = $1 AND state = $2 RETURNING id` (state doubles as the compare-and-swap guard) rather than a separate read-then-write, so two concurrent transitions on the same id can't race — the loser's zero-row result is indistinguishable from an unknown id or an already-illegal transition, which is exactly the boolean contract the API route expects.
- **Brief tab is the default landing view**: `useDashboardTable`'s `activeTab` starts at `"brief"` (was `"feeds"`); every other tab, including Feeds, remains one click away and behaviorally unchanged.
- **Dup-group retention safety**: `articles.dup_group_id` FK is `ON DELETE SET NULL` — `sweepRetention`'s 30-day delete on a dup-group head is never blocked by newer member rows (up to 48h younger) still referencing it.
- **Entity resolution order**: DB registry (canonical_name or any alias, normalized, all statuses including dismissed) → static dictionaries (creates the entity row on first hit) → an LLM candidate judged `prominence: 'famous'` (creates the entity row directly too — see the famous-auto-accept invariant below) → otherwise accumulates into `entity_candidates`. Dismissed entities still resolve and get mentions recorded (so they never re-surface as candidates) but are excluded from every user-facing list.
- **Entity-pass idempotency**: `processNewArticles` selects cluster heads from the last 6h whose `articles.entities_processed_at` is `NULL` — an explicit marker set as the last statement of a successful run, not a `NOT EXISTS(article_entities)` check (an article whose extracted names are all unresolved candidates never gets an `article_entities` row, so `NOT EXISTS` would keep re-selecting — and re-accumulating into `entity_candidates` — for that article's whole 6h lookback window). Re-runs and catch-up runs self-heal: a crash between the aggregate writes and the marker `UPDATE` leaves that batch unmarked, so it retries next run (additive rollups double-count for that one batch — rare and bounded, preferred over silently dropping the batch).
- **One mention per article per entity**: `dedupeMentions` collapses two resolved mentions of the same entity from one article (possible when two of its aliases both appear in the text) before `article_entities`/`entity_mentions_hourly`/`entity_edges` are written, so a single article can't double-count itself into an hourly bucket's mentions/sentiment_sum.
- **Detector scope and bootstrap guard**: every detector query is scoped to `entities.status = 'tracked'` and cluster-head articles (`dup_group_id IS NULL`) only. First-seen novelty and novel-edge detection both suppress candidates within 72h of the global `MIN(first_seen_at)` across all tracked entities/edges respectively, so the one-time initial dictionary-import cohort never floods the signal queue; the same guard is reused by `/api/brief`'s `newEntities`. Rate surprise is skipped (cold start, not "no surprise") for any entity with under 3 days of baseline history in the trailing-14-day-excluding-last-24h window.
- **News time vs. watch time, and the system warm-up gate**: NEWS TIME (`published_at`, when an article was written) and WATCH TIME (`articles.first_seen_at`, when this platform first observed it) are different clocks. Feed pre-loads backfill ~7 days of publish-dated articles on day one, so anything keyed on news time looks like it has a week of history immediately. `entities`/`entity_edges` `first_seen_at`/`last_seen_at` are watch time (`arrivalAt` in `entity-ingest.ts`) — `entity_mentions_hourly` buckets and `entity_candidates` timing deliberately stay on news time (`effectiveAt`), since publish-date spread is the correct shape for trend charts and legitimate recurrence evidence respectively. `detectors.ts`'s `getSystemEpoch` (`MIN(articles.first_seen_at)`, unscoped by `dup_group_id`) anchors a system-wide warm-up gate (`settings.warmup_days`, default 7) covering ALL FIVE detectors, not just the three originally thought "history-dependent": `runDetectors` checks warm-up first and returns zero signals of every type — without running any other query — while it's active, since `cross_category`'s and `sentiment`'s own trailing-14-day baselines need the same real operating history `surge`/`first_seen`/`novel_edge` do (independent of and in addition to the 72h bootstrap guard above, which alone isn't sufficient once entity `first_seen_at` reflects arrival: a fresh entity's arrival is always "recent," so the bootstrap guard's own anchor is equally fresh). The surge baseline's daily rate divides by `computeEffectiveBaselineDays` (real elapsed operating days, clamped to `[1, 14]`), not a fixed 14-day divisor — the fixed divisor understated λ for a system that hadn't actually been running that long.
- **Deviation-based cross_category and sentiment**: both fire on a deviation from the entity's OWN trailing-14-day baseline (excluding the last 24h), never an absolute level. `cross_category`'s baseline is the average distinct source-category count per ACTIVE day (a day with ≥1 mention, from `article_entities` × cluster-head `articles`); it requires ≥3 active baseline days (else the entity is skipped) and fires when `categoryCount24h >= 4` AND `excess = categoryCount24h - baselineAvgCategories >= 2`. `sentiment`'s baseline is average sentiment (`sentiment_sum/mentions`) over the same window from `entity_mentions_hourly`; it requires ≥3 baseline days with mentions AND ≥10 baseline mentions, and fires when `mentions24h >= 5` AND `delta = avg24h - baselineAvg <= -0.3` — a DROP from the entity's own norm, regardless of how negative that norm already is. This replaced an earlier design that fired on absolute category count (≥3) and absolute sentiment (≤-0.3), which in production meant a perpetually multi-category entity (e.g. a major country) or a perpetually negative one (e.g. an active war zone) tripped the detector every single run — see the DEVLOG entry for the concrete Iran/Black-Sea failure mode this fixed.
- **No detector fires on an absolute level. Every signal is a deviation from the entity's own baseline, and no history-dependent computation runs during warm-up.**
- **Candidate promotion rule**: the Review queue shows only candidates with ≥3 distinct sources, ≥2 distinct days, last seen within 14 days.
- **Famous-entity auto-accept**: an LLM candidate with no registry match and `prominence: 'famous'` (the LLM's own top confidence tier — a household name a general news reader needs no explanation for) is inserted directly into `entities` as `status: 'tracked'` (type from the LLM, `first_seen_at`/`last_seen_at` = article ARRIVAL time) instead of accumulating in `entity_candidates`, so it never occupies the human review queue. `'known'` and `'obscure'` LLM candidates, and every heuristic-layer candidate (which never carries a `prominence` judgment at all — only the LLM layer sets it), are unaffected and still queue normally. Counted separately in `EntityIngestStats.entities.autoAccepted` (a subset of `newEntities`).
- **LLM-fallback invariant**: `processNewArticles` never depends on the LLM succeeding. Every LLM failure mode — not configured, budget exhausted, network error, timeout, non-2xx response, unparseable JSON — is caught inside `llm-extract.ts` and surfaces as `extractEntitiesBatch` returning `null`; `entity-ingest.ts` then falls back to the unchanged full heuristic stack (`extractCandidates`) for the affected articles, logs via `console.warn`/`console.error`, and continues the run. The one exception (`safeLlmMonthCost`) wraps even the purely informational end-of-run `llm` stats read in its own try/catch, because it runs after `markArticlesProcessed` — a throw there must not make an already-committed, already-marked run report as failed.
- **LLM budget-cap invariant**: `extractEntitiesBatch` reads `llm_usage` for the current UTC month (`YYYY-MM` key) before every API call and estimates cost via `estimateCostUsd` (Haiku 4.5 list pricing, $1/$5 per MTok input/output — verify against current pricing before changing). At or over `settings.llm_monthly_budget_usd` (default $5), it skips the call entirely (`console.warn`, returns `null`) — no call, no spend, no partial charge. `entity-ingest.ts` reads the budget once per run (`getSettings`) and reuses it across every chunk's `extractEntitiesBatch` call, so cost only ever accumulates, never resets, within a run. `ANTHROPIC_API_KEY` is never logged or echoed by any code path (request headers, `console.warn` messages, or thrown errors) — see `llm-extract.test.ts`'s "never logs the API key on any failure path" test.
- **entity_edges pair ordering**: always stored as `entity_a < entity_b` (by id) — enforced by a DB CHECK and by `rollupEntityEdges` sorting ids before pairing.
- **Relations are directed, typed, and evidence-linked**: unlike `entity_edges` (undirected co-occurrence counts, `entity_a < entity_b`), `entity_relations` preserves the LLM's stated `source_id`/`target_id` direction and a specific `relation` type (PK `(source_id, target_id, relation)` — A→B and B→A, or two different relation types between the same pair, are distinct rows). A relation is only ever written once BOTH its endpoints resolve to a known entity id in the same run (pre-run registry, by norm/alias/any status, overlaid with that run's own fresh inserts — dictionary and famous-auto-accept alike); an endpoint that's still an unresolved candidate drops the relation for that run with no retry queue — it reappears automatically if the same story (or a follow-up) recurs after that endpoint becomes tracked. A resolved self-loop (both names normalize to the same entity via alias overlap) is dropped before the write, since the DB `CHECK (source_id <> target_id)` would otherwise fail the whole batched upsert. On conflict, `article_count` accumulates, `last_seen_at` bumps to the newest ARRIVAL, and `evidence_article_id` is unconditionally replaced with that run's newest supporting article; `first_seen_at` is set once on first insert and never revised.
- **Edge history retention**: 30-day window; entries older than 30 days are pruned on save.
- **Entity snapshot window**: 2-hour retention for emergence detection.
- **Fetch timeout**: 5 seconds per phase. Direct phase allows 1 retry on 5xx. Total worst-case per source: ~20s across all phases.
- **Cache layers**: In-memory feed cache (5min fresh / 30min stale) → CDN (60s fresh / 300s stale-while-revalidate) → browser (no-store). Next.js Data Cache is force-disabled.
- **Ad filtering order**: URL patterns → title regex (50+ patterns) → financial ad regex (dateless feeds only). Applied before items enter the pipeline.
- **CI test gate**: `.github/workflows/test.yml` runs on every PR and every push to `main` — `tsc --noEmit`, the full `vitest` suite (unit + integration) against a real Postgres 16 service container, then `npm run build`. Nothing merges without all three passing.
- **Integration-test layer**: `src/lib/server/__tests__/integration/*.test.ts` exercises `persistArticles`, `sweepRetention`, `processNewArticles`, and the full signal engine (`runDetectors`/`persistSignals`/`transitionSignal`/`loadSignals`/`getBrief`) against a real Postgres (via `pg`, not `@neondatabase/serverless` — the Neon HTTP driver requires Neon's proxy and cannot reach a plain Postgres), gated by `describe.skipIf(!process.env.TEST_DATABASE_URL)` so the suite skips cleanly with no database configured (this sandbox, teammate laptops) and runs for real in CI. An ungated canary test fails loudly if `CI` is set without `TEST_DATABASE_URL`, so a misconfigured CI env can't silently skip the whole layer while staying green. Each integration test file owns its own named Postgres schema (set via the pool's `search_path` option, torn down and reapplied from the real `migrations/*.sql` files before every test) rather than sharing `public` — vitest runs test files concurrently against the one shared `TEST_DATABASE_URL`, so schema-per-file avoids one file's drop/recreate racing another's mid-test.

## Boundaries

**Browser → Server**
- `useFeed` hook fetches `GET /api/articles` (DB-backed) first; on a 503, network error, or malformed response it falls back to `GET /api/sources` (live), both with `cache: 'no-store'`
- Response includes `items[]`; the live path also includes feed diagnostics and metadata, the DB path includes `lastIngestAt`
- `useFeed`, in db mode, runs an attention-driven passive freshness check — on load, on `visibilitychange` to visible, and on a ~5min interval while visible — firing `POST /api/tick` (passive, no `?manual=1`) whenever `lastIngestAt` is over 15min stale; silent, never touches `refreshState` (see the self-healing invariant)
- `useFeed`'s `refresh()` (the Refresh button, via `HeaderBar`/`useDashboardTable`), in db mode, POSTs `POST /api/tick?manual=1` and drives `refreshState` off the response (see the honest-refresh invariant); live mode's `refresh()` re-fetches `/api/sources` directly, unchanged
- `useDashboardTable`'s `handleEntityClick` fetches `GET /api/entities?name=`; on a hit it opens `EntityPanel` (which then fetches `GET /api/entities/[id]`); on 404/malformed/network failure it warns and falls back to the text filter. `EntitiesTab`'s row name-click skips this resolution entirely — the row already carries its own id — and opens `EntityPanel` directly via `onEntitySelect`
- `useCandidateQueue` (EM-2b, absorbs the deleted `ReviewTab`) fetches `GET /api/candidates` on mount; `POST /api/candidates` for single accept/merge/dismiss, `POST /api/candidates/bulk` for bulk accept/dismiss (both unchanged endpoints); its `SampleTitleLink` children fetch `GET /api/articles?titleExact=` on click to resolve a sample title to its article link
- `useDashboardTable` fetches `GET /api/candidates` once on mount for the header badge count; `useCandidateQueue` syncs it after every load (mount + after each action) via the same `onCandidatesChanged`-shaped callback `ReviewTab` used to drive, now re-pointed through `EntitiesTab`
- `useBriefTab` fetches `GET /api/brief` on mount; `POST /api/signals` for the Seen/Promote/Dismiss card actions, then re-fetches the brief
- `useSignalsTab` fetches `GET /api/signals?state=new,seen,dismissed,promoted` on mount (all states, filtered client-side); `POST /api/signals` for Seen/Promote/Dismiss/Reopen, then re-fetches

**Server → External Feeds**
- `fetchAllFeeds` hits ~147 RSS/Atom sources in parallel — called from `/api/sources` (live reads) and `run-ingest.ts`'s `runIngest` (writes, invoked by both `/api/ingest`'s hourly/daily crons and `/api/tick`'s self-heal)
- 3-phase fallback per source: direct fetch → `RELAY_URL` proxy → `altUrl`
- Optional `RELAY_SECRET` header for relay authentication
- All fetches use `cache: 'no-store'` to bypass Next.js Data Cache

**Server → External LLM**
- `llm-extract.ts` calls the Anthropic Message Batches API (`POST`/`GET https://api.anthropic.com/v1/messages/batches[/...]`, plus a `GET` of the API-returned `results_url`) directly via `fetch` (no SDK), only from `entity-ingest.ts`'s `processNewArticles` when `isLlmConfigured()` (`ANTHROPIC_API_KEY` set) and the monthly budget isn't exhausted (checked at submit time)
- Plain `fetch`, 12s timeout via `AbortController` per call (submit/poll/results-fetch are each their own bounded call, at most one per ingest tick), no retries (the next `:07`/`:37` ingest tick is the retry — extraction-derived data can land one tick later than article arrival, per DESIGN.md's honest-time spine)
- The sandbox and CI have no `ANTHROPIC_API_KEY` and must never make a real call — every test that exercises this path stubs `fetch`, with a per-test guard that throws if an un-mocked call is attempted

**Server → Postgres**
- `/api/ingest` (GitHub Actions cron, two schedules per hour + daily Vercel cron fallback) and `/api/tick` (unauthenticated self-heal) both call `run-ingest.ts`'s `runIngest`, the sole writer for articles/entities/signals: fetches all feeds, `persistArticles` (batched insert + dup-group linking), `sweepRetention`, `processNewArticles` (entity resolution + rollup writes, incl. `llm_usage` upserts when the LLM path runs), then `getSettings` → `runDetectors` → `persistSignals` (detect-and-persist signals) as the final stage, then a read-only `compute-warmup` stage for the response
- `/api/tick` additionally reads `MAX(articles.first_seen_at)` for its freshness check and reads/writes the `settings.tick_lock` row for its anti-stampede lock (`tick.ts`) before ever deciding whether to call `runIngest`
- `/api/candidates` POST and `/api/signals` POST are two other writers: accept/merge/dismiss (ending in a delete from `entity_candidates`) and signal state transitions respectively. `/api/candidates/bulk` POST reuses `/api/candidates`'s own exported accept/dismiss functions per item, capped at 100 items. `/api/entities/[id]` PATCH is the fifth writer: a single `COALESCE`-based `UPDATE ... RETURNING` applies any subset of `{type, status, fame, fame_locked}`
- `/api/articles` reads cluster heads (`dup_group_id IS NULL`) from the last N days, capped at 500, with each cluster's `updated_at` and `cluster_size` from the `cluster_updates` CTE
- `/api/entities` resolves a name to an entity id (or, in list mode, searches/paginates via `entity-admin.ts`'s `searchEntities`); `/api/entities/[id]` GET reads that entity's profile, hourly series, articles, edges, and incoming/outgoing entity_relations
- `/api/candidates` GET reads promotable candidates
- `/api/signals` GET reads signals in the requested states via `loadSignals` (shared with `/api/brief`); `/api/brief` reads active signals (capped at `settings.brief_max_blocks`), new entities, and ranked top stories via `getBrief`
- `scripts/migrate.mjs` (manual, owner-run) applies `migrations/*.sql` against `DATABASE_URL`
- All access goes through `getSql()` (`src/lib/server/db.ts`) — no module-level client, no shared connection state

**Server → CDN**
- Vercel Edge caches API responses (`s-maxage=60, stale-while-revalidate=300`); `/api/brief` uses its own `s-maxage=300, stale-while-revalidate=600`

**Client → External Services**
- Google Favicons API (`google.com/s2/favicons`) for fallback article images
- CARTO tile CDN (`basemaps.cartocdn.com`) for Leaflet map layers
- Google Fonts CDN for Inter typeface

**Client → localStorage**
- `signal-storage.ts` reads/writes: edge history, entity snapshots, entity baselines (muted entities removed — superseded by server-side dismiss)
- Theme preference stored as `wd-theme`; a one-time `useSignalsTab` effect removes the now-obsolete `wd-muted-entities` key

**Internal Module Dependencies**
- `DashboardTable` → `useDashboardTable` + `HeaderBar` + `EntityPanel` + dashboard sub-components
- `useDashboardTable` → `useFeed` + `theme` + `/api/entities` (name resolution) + `/api/candidates` (badge count)
- `TabContent` → all tab components (dynamic imports) + `BriefTab` + `EntitiesTab`
- `BriefTab` → `useBriefTab` + brief sub-components (`BriefMoversSection`, `ManagedSignalCard` from `signals/`)
- `SignalsTab` → `useSignalsTab` + signals sub-components (`StateFilterBar`, `ManagedSignalGrid` → `ManagedSignalCard`)
- `EntitiesTab` → `useEntitiesTab` + `useCandidateQueue` + `entities/` sub-components (Tracked table + candidate-queue segments, EM-2b)
- `useCandidateQueue` → `/api/candidates` (GET/POST) + `/api/candidates/bulk` (POST) — absorbs the deleted `ReviewTab`'s dependency
- `SampleTitleLink` (`entities/`) → `/api/articles` (`?titleExact=` lookup)
- `EntityPanel` → `/api/entities/[id]`
- `/api/ingest` → `run-ingest` (`runIngest`)
- `/api/tick` → `run-ingest` (`runIngest`) + `tick` (`getLastIngestAt`, `tryAcquireLock`, `isRecent`)
- `run-ingest` → `entity-ingest` (`processNewArticles`) after `ingest-writer`, then `settings` (`getSettings`) → `detectors` (`runDetectors`) → `signal-store` (`persistSignals`), then its own `compute-warmup` stage (`detectors`'s `computeWarmupState`)
- `entity-ingest` → `extract-v2` + `entity-extractor` (`scoreSentiment`) + `llm-extract` (`isLlmConfigured`, `submitBatch`, `pollBatch`, `fetchBatchResults`, `recordBatchUsage`, `getLlmMonthStats`, `currentUtcMonth`, `MAX_BATCH_SIZE`) + `settings` (`getSettings`, for the LLM budget)
- `llm-extract` → `extract-v2` (`Candidate`, `TypeHint`, `addCandidate` — builds LLM-sourced candidates through the same dedup/priority merge as every other layer)
- `detectors` → `settings` (`Settings` type)
- `brief` → `detectors` (`computeWarmupState`, `computeEffectiveBaselineDays` — imports only, no changes to that file)
- `signal-store` → `detectors` (`CandidateSignal` type) + `settings` (`Settings` type)
- `brief` → `signal-store` (`loadSignals`) + `settings` (`Settings` type)
- `/api/signals`, `/api/brief` → `signal-store` (+ `brief` for the latter)
- `extract-v2` → `entity-extractor` (`matchDictionaryEntries`, `isDictionaryTerm`) + `entity-dictionaries` (`PERSON_STOPWORDS`) + `compromise`
- `/api/entities`, `/api/entities/[id]` → `extract-v2` (`normalizeName`) + `entity-admin` (`searchEntities`, `ValidationError`, `toEntityAdminJson`)
- `/api/candidates`, `/api/candidates/bulk` → `candidate-actions` (`loadCandidate`, `acceptCandidate`, `dismissCandidate`, `inlineFameCheck`, `isBoundedNonEmpty`, `TYPES`, `CONFLICT_MESSAGE`, `deleteCandidate`)
- `candidate-actions` → `fame-sweep` (`checkAndWriteEntityFame`)
- `FeedTable` / `FeedCardList` → `FeedItemImage` + `date-utils`
- All analysis tabs → `entity-extractor` → `entity-dictionaries` + `urgency`
- `useSignalsTab` → `/api/signals` (manager)
- `useEnrichedEntities` → `novelty-scorer` + `signal-storage` (unreferenced now — no caller since `useSignalsTab`'s watchlist was removed)
- `MapTab` → `geo-coordinates` + `react-leaflet`
- `NetworkTab` → `react-force-graph-2d`
- `feed-fetcher` → `ad-filter` + `xml-helpers` + `image-extractor`
